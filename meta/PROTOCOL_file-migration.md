# PROTOCOL_file-migration — протокол П-1, редакция 3

Источник: наряд Н-03.09-11 (Руководитель, 2026-09-03).
Провенанс наряда: ПРОВЕРЕНО ПО PROJECT_LOG_SYS: шапка N=55 | хвост Т-16 от 2026-09-03 | релевантные решения: Р-21, Р-26, Р-28, Р-31 | тупики: Т-05, Т-11, Т-15, Т-16.

## Тело протокола — дословно из наряда Н-03.09-11, поле ЗАДАЧА (Ш0/Ш2/Ш3/Ш3-список)

Ниже — точная копия текста наряда, без перефразирования и сокращения.

> Ш0. Паспорт носителя PLAN. Известное: Читатель — сессия Руководителя миграции, триггер «начало работы над миграцией», gdocs-read; не прочёл — работает по устаревшей шапке (прецедент 02–03.09). Писатель — сессия по наряду, момент закрытия фазы, gdocs-replace, конфликт не разрешается ничем. Строку АДРЕСА собрать по объекту: CORE Р.2 и Зона А SYS_INDEX `12k2l2oT92m33PFcnlYDr1MdtkGOj7Gy5Vf7ksSw4nm8` до строки 648, найденное цитировать дословно.
>
> Ш2. Выписать механику PLAN. Заведомо: `МАРКЕР ВСТАВКИ ЖУРНАЛА` → раздел «контракт записи» протокола.
>
> Ш3. Записать два файла. `/meta/PROTOCOL_file-migration.md` — текст протокола П-1 редакции 3 дословно из этого наряда. `/projects/git-migration/PLAN.md` — смысл PLAN по правилам Ш3: только действующее, переформулированное кратко и однозначно. Заведомо не переезжают: раздел 11 (журнал хода — след), раздел 9 (конфликт снят, сам документ зовёт его историческим), пункты разделов 6, 10 и 12, отменённые разделом 14, и подпункты 14.1, ставшие ненужными после исправления шапки. Заведомо переезжают: инварианты И1–И5, алгоритм триажа, запрет Б-1 целиком, список отвергнутого 14.2, шаблон наряда на перенос, текущее состояние работ, открытые вопросы 14.4, находки 12.x, на которые ещё ссылаются.
>
> Ш3-список. Отдельным разделом в конце нового файла — таблица невзятого: что, почему не взято, адрес восстановления (документ и раздел). Ноль записей «неизвестно».

## Контракт записи — исполнение Ш2 (составлено Прорабом при исполнении, не дословная цитата)

Позиция ниже помечена самим Ш2 как «заведомая»: `МАРКЕР ВСТАВКИ ЖУРНАЛА` → этот раздел.

Найдено при чтении источника 2026-09-03 (gdocs-read, PLAN_git-migration_CANDIDATE_2026-09-01, fileId `184yBN_w3trhBUDP_7SdzVIM6zVvdPajo_bW_p6LSfro`, byteLength 46124, checkSum 53a3eaf5), строка 113, между разделом «ОТВЕРГНУТЫЕ АЛЬТЕРНАТИВЫ» и заголовком раздела 11, дословно:

> МАРКЕР ВСТАВКИ ЖУРНАЛА

Это литеральный якорь для gdocs-replace: место, куда старый (Docs) писатель вставлял новую запись раздела 11 «ЖУРНАЛ МИГРАЦИИ». Маркер — свойство Docs-носителя (позиция внутри одного файла, найденная по тексту), физического аналога у git-канала нет.

Контракт записи в git-канале (взамен маркера, по разделу 5 источника, механика подтверждена этой же сессией exec-парой git-read/git-write 2026-09-03): запись — не вставка по анкеру, а полная перезапись файла с последующей независимой проверкой. Порядок: (1) независимый git-read текущего файла, снять `sha`; (2) собрать новый текст целиком (старое содержимое + новая запись); (3) git-write `{path, newText, expectedSha=<sha из шага 1>, message}` — несовпадение `expectedSha` на сервере даёт явный отказ вместо тихой потери (класс Т-05 источника, раздел 5.2, закрыт архитектурно самим GitHub); (4) верификация — независимый git-read по тому же пути + локальный пересчёт blob SHA-1, сверка с `newSha` ответа (И3 источника).

Адрес восстановления маркера: PLAN_git-migration_CANDIDATE_2026-09-01 (184yBN_w3trhBUDP_7SdzVIM6zVvdPajo_bW_p6LSfro, статус — заморожен по И1), строка 113.
