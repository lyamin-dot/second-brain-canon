meta/PROTOCOL_harvest.md

# Харвест — перенос опыта проектов и отложенных правок в Skills

Заведён 2026-09-16 решением Р-90 журнала `projects/git-migration/LOG.md`, дополнен тем же днём решением Р-91 (очередь правок и перепроверка правил). Заменяет маршрут ХАРВЕСТ цикла обслуживания CORE Раздела 12, снятого решением 2026-09-07 (Р-89). От прежнего цикла взята только функция. Повестка, поле ЦИКЛ, контрольная карта, критерии успеха и пороги запуска не взяты.

Кто читает и когда: сессия проекта SYS по слову владельца «харвест». Других запусков нет.

последний харвест: 2026-08-31

## Шаги

1. Снять дату последнего изменения (get_file_metadata) со всех INDEX проектов из `registry/documents.md`, блоки T-007 и T-009. Полностью прочитать через gdocs-read только те, что изменены позже строки «последний харвест». В ответ — список прочитанных и список пропущенных с датами.
2. В прочитанных INDEX — секции НАКОПЛЕННЫЙ ОПЫТ и РЕШЕНИЯ, только записи новее строки «последний харвест». Отобрать уроки, которые повторяются в двух и более проектах или отсутствуют в действующих Skills. Проверка отсутствия — поиск формулировки в тексте Skill, а не память.
3. Очередь правок `registry/maintenance-queue.md`: найти позиции, чей статус не означает внесение, снятие, закрытие или дубль, а дата смерти наступила или наступит до ближайшего понедельника. Искать по строкам таблиц (Git Read Lines с pattern по номеру позиции и по дате; в среде с клоном — grep. Git Grep для этого шага ЗАПРЕЩЁН: он несёт в contextData base64 всего файла, nodeNames его не режет, поэтому поиск по очереди стоит дороже, чем чтение очереди целиком — Р-2 `meta/CONTRACT_file-limits.md`, exec 63960), а не по разделам «Счёт» — счёты в файле не сходятся (`labs/sys/t-queue-count.md`). Для каждой позиции — полный текст правки «было → стало» либо вопрос владельцу: вносим, переносим дату или снимаем.
4. Правила с меткой `[verified: ГГГГ-ММ-ДД]` старше 90 суток в Skills `second-brain-engineer`, `second-brain-git`, `n8n-engineering-manual`, `google-drive-docs`, `n8n-workflow-passport` — список кандидатов на перепроверку. Правила не удалять и не переписывать. Нет таких — написать «ноль» явно.
5. Выдать владельцу одним пакетом всё найденное на шагах 2–4: имя Skill или файла, якорь-строка, полный блок «было → стало» либо вопрос. Правки Skills и мастер-промпта вносит владелец; правки файлов репозитория сессия вносит сама по `second-brain-git` §8. Если предложить нечего — написать «правок нет» явно.
6. Последним действием заменить дату в строке «последний харвест» через Git Replace (blob sha из свежего чтения) и проверить обратным чтением. Если шаги 1–5 не выполнены — дата не двигается.

## Напоминание

Воркфлоу n8n Gap Monitor (`5Rs6pNDSm4weWDVj`), проверка П1: если дата в строке «последний харвест» старше 14 суток — одно сообщение в Telegram. Перенастройка монитора на этот файл — наряд Н-16.09-10; до его закрытия напоминание на этот файл не работает. Порог 14 суток предложен сессией 2026-09-16 и владельцем отдельно не подтверждён; читатель числа — монитор, момент — его еженедельный запуск. Меняется правкой одного числа здесь и в мониторе.
