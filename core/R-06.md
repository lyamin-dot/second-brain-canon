<!-- T-044 | CORE байт 30306-31057 | Р.6 базовый контракт записи (записать -> прочитать обратно -> отчёт; «Записано» только после верификации) -->
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
РАЗДЕЛ 6 — КОНТРАКТ ЗАПИСИ
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━








1. Записать → 2. Прочитать обратно → 3. Отчёт:
  OK:    верифицировано | файл | время | первые 80 символов
  ОШИБКА: данные НЕ сохранены → остаются в чате как резервная копия








«Записано» сообщается ТОЛЬКО после верификации.


















<!-- T-045 | CORE байт 31058-32276 | Р.6 ШАГ 0 — проверка содержания, п.0.1-0.3 -->
ШАГ 0 — ПРОВЕРКА СОДЕРЖАНИЯ (выполняется ДО записи, 2026-07-28):
0.1 Запись о завершённости обязана нести перечень объектов, к которым она относится, и способ проверки. Нет перечня — писать «выполнено частично, охват неизвестен», а не галочку. Число вместо списка записью о завершённости не является.
0.2 Состояние внешней системы (n8n, БД, Drive, аккаунт) пишется как дата и предмет проверки, не как статус. Статус берётся из объекта в момент использования: объект сам себе источник истины, копия в документе протухает молча.
0.3 Вопрос без критерия закрытия не пишется в ОТКРЫТЫЕ ЗАДАЧИ. Либо ответ получен в этой же сессии, либо это задача с объектом и явным признаком готовности.


<!-- T-046 | CORE байт 32277-32777 | Р.6 п.0.4 — read-back подтверждает факт записи, но не достоверность значения -->








0.4 Read-back подтверждает факт записи (дошли байты), а не достоверность содержания. Для нетекстовых значений (ID, ссылка, техническая деталь) read-back обязан проверить действительность значения независимо (резолвится/существует), а не только присутствие строки в документе.


<!-- T-047 | CORE байт 32778-33662 | Р.6 ШАГ 0.5 — адресация записи + п.0.5.1 (источник типа и проекта) -->








ШАГ 0.5 — АДРЕСАЦИЯ (НОВОЕ, решение 1)
Выполняется после ШАГ 0, до физической записи. Отвечает на вопрос «в какой приёмник пишем», если запись инициирована командой !save.








0.5.1 Тип записи берётся из словаря Р.4 (Задача / Идея / Вопрос / Решение / Проблема / Событие / Результат / Баг). Проект берётся из метки [ПРОЕКТ] формата ЗАХВАЧЕНО (Р.4). Если проект не ясен — [?], маршрутизация не выполняется, запись остаётся в чате (правило Р.4 «не угадывать» не отменяется этим шагом).










<!-- T-049 | CORE байт 34616-35417 | Р.6 п.0.5.3 — каноническая маршрутная таблица тип -> приёмник (наряд Н-14.08-07) -->
0.5.3 Маршрутная таблица заменена канонической (наряд Н-14.08-07, 2026-08-14): Задача с «Готово=» → Зона А целевого INDEX; Баг/Инцидент/Проблема → секция БАГИ целевого INDEX, в момент обнаружения; Урок/Событие/Результат → журнальный корпус проекта (SYS_JOURNAL_2026 / N8N_JOURNAL_2026); Решение → секция РЕШЕНИЯ целевого INDEX с датой; Идея → секция ИДЕИ; Справка → REFERENCE_DOC; Вопрос — только с критерием закрытия (ШАГ 0.3), иначе не пишется; не классифицировано [?] → INBOX.










<!-- T-050 | CORE байт 35418-36280 | Р.6 п.0.5.4 — правило Зоны Б для типов без журнального корпуса -->
0.5.4 Правило Зоны Б для типов без журнального корпуса (закрытие ВОПРОС-3, решение Руководителя, подтверждено Андреем 14.08, наряд Н-14.08-09): для проектов без журнального корпуса типы Урок/Событие/Результат адресуются в Зону Б целевого INDEX (холодное хранилище, читатель — обслуживание). Новый журнальный корпус заводится только одновременно с постановкой в очередь R2 — читатель раньше хранилища. Баг/Проблема пробелом не являются: секция БАГИ есть в каждом INDEX.


















<!-- T-051 | CORE байт 36281-37477 | Р.6 ШАГ 0.6 — предикат входа в Зону А («открытый чек-бокс с критерием Готово =») -->
ШАГ 0.6 — ПРЕДИКАТ ВХОДА В ЗОНУ А (НОВОЕ, решение 2)
Наряд называет само решение («предикат входа в Зону А»), но не даёт его формулировки — ни условия, ни источника. Существующий текст SBE закрепляет только противоположный конец конвейера: «ЗОНА А — читается при каждом старте сессии», «ЗОНА Б — читается только по запросу или при !review» (SYS_INDEX, шапки зон). Это описывает ЧТЕНИЕ, не критерий ЗАПИСИ — то есть не отвечает на вопрос «что именно квалифицируется для попадания новой строки в Зону А при записи».
Принятый предикат: запись является открытым чек-боксом с критерием «Готово =".








Предикат принят и внесён в CORE как рабочее правило.


















<!-- T-052 | CORE байт 37478-38566 | Р.6 ШАГ 0.7 — бюджет Зоны А (метод замера M1, база 21957, порог дельты 10240, ЦЕЛЬ_M1 30720) -->
ШАГ 0.7 — Бюджет Зоны А
Существующий замер M1 (CORE Р.12 п.0): байты UTF-8 Зоны А SYS_INDEX от начала файла до canary-строки PW-SYS включительно; считается len(text.encode('utf-8')) по download_file_content. База 21 957 (30.07). Порог ДЕЛЬТЫ (не абсолютного размера) — 10 240: превышение прироста с базы сигналит на цикле обслуживания. Замер на 13.08 = 48 120 (прирост +26 163 от базы, то есть более чем вдвое сверх порога дельты).
ЦЕЛЬ_M1 = 30 720 байт — верхняя граница АБСОЛЮТНОГО размера Зоны А SYS_INDEX, до которой систему приводят вытеснением; считается тем же методом (len(text.encode('utf-8')) по download_file_content, от начала файла до canary-строки PW-SYS включительно).


















<!-- T-053 | CORE байт 38567-44145 | Р.6 ПРАВИЛО ПОТОКОВОГО ВЫТЕСНЕНИЯ <=2 КБ (с ревизией Н-30.08-20/22) -->
ПРАВИЛО ПОТОКОВОГО ВЫТЕСНЕНИЯ ≤2 КБ (НОВОЕ, решение 4)
Основание: SYS_JOURNAL_2026_02, записи 2026-08-05 (в SYS_INDEX такой секции нет — вынесена в корпус хирургией Н-15.08-05 от 15.08; уточнено нарядами Н-30.08-20/22). В самом источнике формулировка мягче: «вставка текста крупнее ~2 КБ в Doc» названа одной из трёх подтверждённых ручных операций, а числа «практический потолок ~1-2 КБ» как категорического утверждения в источнике нет — оно появилось при переносе в CORE. Основание отпало 2026-08-26 с появлением gdocs-apply-from-file (source читается из Drive-файла и через тело вызова не проходит): заявлено 1 500 000 символов одним batchUpdate (паспорт Н-26.08-03, независимо не перепроверено), независимо подтверждена запись 174 326 Б, отказ Docs API зафиксирован на 1 900 000 — фактическая граница между 1,5М и 1,9М не измерена. Прежний список инструментов (gdocs-append/replace/overwrite v2, MCP create_file) верно описывал состояние на 14.08 и не описывает состояние с 26.08. Массовое удаление у gdocs-replace/overwrite v2 тем же путём по-прежнему не работает (Guard K1, google-drive-docs §15.7 — держится, не опровергнуто), дробить его на серию однострочных replace запрещено; mode=overwrite у gdocs-apply-from-file выполняет полное удаление и перезапись машинно, другим инструментом.
Правило для вытеснения (перенос закрытых/устаревших строк Зоны А → Зона Б или журнал/архив) при исполнении бюджета M1 (ШАГ 0.7):
  — вытеснение выполняется порциями ≤2 КБ за один write-вызов (gdocs-replace для точечного удаления строки с однозначным якорем, см. протокол деструктивного replace google-drive-docs §3.2);
    — для gdocs-append v2 и gdocs-replace v2 числовой потолок операции НЕ ИЗМЕРЕН: объекта с зафиксированным числом не найдено нигде (наряд Н-30.08-20) — это «не зарегистрировано», а не «проверено и подтверждено»; для gdocs-overwrite v2 известна только нижняя граница отказа (порча байт на ~29 КБ, инцидент 31.07), верхняя граница успеха не зафиксирована; как общее утверждение «любая операция» — опровергнуто gdocs-apply-from-file, где полная операция многократно превышает 2 КБ на каждом шаге и работает штатно;
  — вытеснение объёмом больше ~2 КБ выполняется через gdocs-apply-from-file (mode=overwrite, source из Drive-файла, наряд Н-26.08-03): заявленный потолок 1 500 000 символов независимо не перепроверен, независимо подтверждена запись 174 326 Б, отказ на 1 900 000. Машинный путь для текста крупнее тела вызова существует с 2026-08-26; ручной путь §15.6 остаётся резервным для объёмов выше подтверждённого и на случай отказа инструмента;
  — каждая порция подтверждается независимым read-back до перехода к следующей (не пакетно) — иначе теряется провенанс, какая именно порция ушла успешно, а какая нет.
Общего машинного потолка ~2 КБ как факта не существует (наряд Н-30.08-20): цифра относится только к gdocs-append/replace/overwrite v2 и точным числом не измерена ни для одного из трёх; у gdocs-apply-from-file предел иного порядка. Дробление ≤2 КБ сохраняется для gdocs-append/replace/overwrite v2 как КОНСЕРВАТИВНЫЙ ДЕФОЛТ БЕЗ ЗАМЕРА и как дисциплина порционного read-back, а не как отражение измеренного предела. Условие пересмотра: появление объекта с измеренным числом хотя бы для одного из трёх инструментов.
Очерёдность при превышении бюджета: первыми вытесняются записи с низшим итоговым приоритетом по формуле Р.5 (метка × вес проекта).


















<!-- T-054 | CORE байт 44146-46822 | Р.6 обязательное поле-симптом журнальной записи -->
ОБЯЗАТЕЛЬНОЕ ПОЛЕ-СИМПТОМ ЖУРНАЛЬНОЙ ЗАПИСИ (НОВОЕ, решение 5)
Основание: формат «[YYYY-MM-DD] [симптом] → [решение]» уже используется де-факто в SYS_INDEX «Ошибки и решения» и унаследован SYS_JOURNAL_2026 дословно при вырезке 31.07 (CORE Р.2) — но нигде в CORE не закреплён как ОБЯЗАТЕЛЬНЫЙ, это сложившаяся практика, не контрактное правило. Находка Н-14.08-02 (карта потоков записи): читатель error-memory/R2 SYS_JOURNAL_2026 существовал «на бумаге» до 2026-08-13 без подтверждённых прогонов — отчасти потому, что до формализации поле-симптом было необязательным и не гарантировало парсимость записей читателем-скриптом.
Правило для Р.6: запись в любой журнальный корпус (SYS_JOURNAL_2026, N8N_JOURNAL_2026 и любой будущий журнал по аналогии) обязана содержать поле СИМПТОМ отдельной, однозначно вычленяемой единицей — не растворённым в свободном тексте описанием. Формат по умолчанию (пока Андрей не утвердит иной):
  [YYYY-MM-DD] [симптом: <короткая фраза, воспроизводимый признак — то, что искал бы читатель по ключевому словует>] → [решение: <текст>]
Запись без вычленяемого поля-симптома не проходит ШАГ 0 (0.1: «нет перечня — не является записью о завершённости», по аналогии тот же принцип распространяется на журнал — нет симптома, нет пригодной для машинного чтения записи) и не пишется в журнал этим контрактом; допускается только в чат/черновик.
Поле-симптом обязательно для записей с адресом «журнал» (см. маршрутную таблицу ШАГ 0.5.3/0.5.4); для БАГИ симптом входит в формат записи; для остальных приёмников не требуется.









