<!-- T-085 | CORE байт 81842-83322 | Р.12 п.10а МАРШРУТ ПО ТРИГГЕРУ + п.10 ПРОТОКОЛ v4-MVP -->
10а. МАРШРУТ ПО ТРИГГЕРУ (2026-08-23): цикл ХАРВЕСТ исполняет шаги 1-2-3-5-6; цикл ВЫЧИТАНИЕ исполняет шаги 0-4. Шаг 7 назначается Андреем отдельно, вне обоих маршрутов. Правила п.7 и п.8 применяются к строкам своего маршрута, не всей повестки.
10. ПРОТОКОЛ v4-MVP — одна сессия на маршрут (2026-08-25): ВЫЧИТАНИЕ — одна сессия, шаги 0–4, по PROTOCOL_maintenance_v4 (CORE Р.2) фазы Ф0–Ф3; ХАРВЕСТ — одна сессия, шаги 1-3-5-6; шаг 2 при непустом харвесте — отдельная сессия Opus вне цикла (п.9). Старт любой сессии = v4 Ф0, в любой среде. Контракт чтения канона задаётся НОСИТЕЛЕМ читаемого объекта, а не средой сессии (правка 2026-09-07): объект в repo закрывается core/MANIFEST.md — число прочитанных файлов списка, путь последнего файла списка и его blob sha из ответа git-read; объект в Docs закрывается хвостовым паролем-канарейкой и пересчётом FNV по gdocs-read. Пароль-канарейка и FNV к файлам repo не применяются и основанием для отказа там не являются: у набора файлов нет конца, на котором держалась канарейка (core/MANIFEST.md; second-brain-git §6; Р-44 и Р-45 журнала projects/git-migration/LOG.md). Набор шага 4 выводится субагентом от правила отбора без контекста родителя и сверяется с контрольными числами шага 3. Цикл закрыт только при заполненном поле «касаний» в КОНТРОЛЬНОЙ КАРТЕ; 4 и больше = (в) ОТКЛОНЁН. АНДРЕЙ: Save skill после шага 5. Провенанс: AUDIT_2026-08-16 П-07/Р-5, Н-17.08-09, v4 (19.08), REPORT_Н-24.08-06. Сетка ЧАТ 1/3/4/5 (v3, 04.08) снята.
10б. ЗАКРЫТИЕ Ф0 = ОТКРЫТИЕ ЦИКЛА ЗАПИСЬЮ (2026-09-07, подтверждено Андреем): Ф0 не считается закрытой, пока цикл не открыт записью в Раздел 12 по п.8 — поле ЦИКЛ (дата триггера + маршрут, R-12b.md) и сброс строк своего маршрута в «не начат» (R-12c.md / R-12d.md) одним атомарным вызовом git-commit, плюс строка «цикл открыт [дата] | среда [Project/Cowork/Code] | маршрут [ХАРВЕСТ/ВЫЧИТАНИЕ]» под полем ЦИКЛ. Запись делается ДО любой диагностики, до metadata по INDEX и до чтения реестра протоколов. Если цикл уже открыт (дата ЦИКЛ равна или новее триггера) — записи нет, сессия продолжает с первого незакрытого шага (п.8, исключение). Основание: 2026-09-07 две сессии подряд прочитали CORE и SYS_INDEX целиком и оборвались, не тронув ни одной строки Раздела 12 — по п.1 их работа для протокола не существовала. Первая запись, стоящая до работы, превращает п.1 из запрета в механизм: обрыв после неё оставляет след, и следующая сессия продолжает, а не начинает с нуля. Отвергнуто: урезать чтение CORE для ХАРВЕСТА (ломает §5.18г Skill; после ф4 резать дважды); двухсессионная сетка (снята 25.08); монитор незакрытых циклов.
 


<!-- T-087 | CORE байт 83598-84093 | Р.12 п.11 Долг архивации (список ОЖИДАЕТ АРХИВА каждого активного INDEX) -->
11. Долг архивации: на шаге read-only диагностики — прочитать список «ОЖИДАЕТ АРХИВА» каждого активного INDEX по имени файла и перенести все якоря в АРХИВ ЗАДАЧ, независимо от статуса размера аккумулятора (§5.16 Skill эту метрику не отслеживает). Введено наряд Н-10.08-05, 2026-08-10.









