projects/anis-evidence/LOG.md

Журнал устройства проекта: решения, тупики, ошибки. Клинические вердикты сюда не пишутся — их носитель `e-<тема>.md` по `PLAN.md`.

Т-01 | 2026-09-13 | правка одной строки в `registry/projects.md` набором полного текста файла из вывода чтения
  почему не вышло: набранная копия разошлась с оригиналом — 6 137 Б против 6 356 Б, blob sha не совпал; при наборе потеряна одна фраза абзаца о переносе Р-66
  условие возврата: набирать текст существующего файла repo вручную — только если `git clone` в среде исполнения недоступен
  стоимость попытки: одна сборка файла и одна сверка хешей; записи не было, репозиторий не тронут
  вывод сессии: `git clone` из облачной песочницы открыт на чтение (§9.1), поэтому точные байты берутся из клона, а `git hash-object` ловит расхождение до записи, а не после — расхождение 219 Б было обнаружено именно так

Т-02 | 2026-09-13 | Git Append в `meta/PENDING_RULES.md`
  почему не вышло: GitHub отбил PUT с ответом «Repository rule violations found / Timed out validating rule»
  условие возврата: наступило в той же сессии — повтор без единой правки прошёл, 2 372 → 3 417 Б
  вывод сессии: отказ по таймауту валидации ruleset временный и не запускает процедуру 8.5, потому что ответ сам содержит доказательство нетронутости файла — `sha_before` равен `sha_after`, `bytes_before` равен `bytes_after` и равен 2 372 при ожидаемой дозаписи 1 045 Б


Р-0001 | В СИЛЕ | 2026-09-13 | устройство канона и опроса
  принято: пять ролей вместо четырёх (EVIDENZ.md и e-<тема>.md), сквозная нумерация записей по проекту вместо нумерации внутри файла, потолок PLAN.md 16 384 Б вместо 10 240 Б, четыре ярлыка опроса со сроками 7/30/90 дней, язык разговора — немецкий
  почему роли: закрытие задачи лаборатории есть удаление её файла (4б.6 second-brain-git), а клинический вердикт удалять нельзя — он отменяется новой записью, но остаётся в истории
  почему сквозные номера: при разрезе файла темы по 6.3 запись переезжает в новый файл, и ссылка вида «E-03 файла e-geburtshilfe.md» указывала бы на файл, которого больше нет
  почему потолок PLAN.md: файл несёт правила опроса и разбора, а читается редко — только сессией, берущейся за устройство проекта
  почему календарные сроки, а не счётчик опросов: счётчик не возвращает запись в редкой теме никогда, календарь гарантирует возврат
  кто подтвердил: Андрей 2026-09-13
  отклонено: слияние с ANIS_Spiegel — очередь там стоит на входе, здесь порождается из уже полученных ответов; интервалы-счётчики; строка со счётчиком созревшего в начале сессии; право агента оценивать ответ владельца; хранение сгенерированных вопросов как второго носителя того же знания

Р-0002 | В СИЛЕ | 2026-09-13 | строка 4 meta/PENDING_RULES.md заменена, а не дополнена
  принято: старая редакция строки 4 переписана на месте
  почему: И-6 объявляет файл append-only, но строка 4 не произвела ни одного артефакта — файлов тем и записей на момент правки не существовало. Отменяющая строка добавила бы около 900 Б к файлу без единицы нового знания
  обратимость: полная, прежняя редакция восстанавливается из истории коммитов
  кто подтвердил: Андрей 2026-09-13

Т-03 | 2026-09-13 | запись PLAN.md и EVIDENZ.md через Git Commit Files
  почему не вышло: два вызова подряд отбиты на стадии parent_check — параллельная сессия ротировала projects/git-migration/LOG_ARCHIVE.md и двигала голову ветки чаще, чем один раз в 15 секунд; окно между чтением головы и вызовом записи у внешнего агента — десятки секунд
  условие возврата: Git Commit Files применим, когда ветка стабильна — два чтения головы с разницей 30 секунд дают один и тот же sha
  стоимость попытки: два вызова по 14 КБ полезной нагрузки; репозиторий не тронут, ответ содержал «Nothing was written» и это подтверждено независимым чтением blob sha
  вывод сессии: у Git Commit Files expectedSha — голова ветки, у Git Write — blob sha самого файла. При чужой активности в других путях первый отбивается бесконечно, второй проходит с первого раза — в этой сессии так и произошло