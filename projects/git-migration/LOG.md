projects/git-migration/LOG.md

# LOG_git-migration — журнал, находки и открытые вопросы

## Находка 12.9 [источник: раздел 12.9 — единственная находка 12.x с явной ссылкой из другого действующего раздела источника (раздел 13)]

12.9 Паспорт стоимости, посчитанный Руководителем, систематически занижает N: Руководитель не знает ограничений инструмента (наблюдалось 15 вместо фактических 21). Рабочее лекарство: Прораб пересчитывает паспорт от факта до начала работ.

## Открыто [источник: раздел 14.4]

T-063 (блок ИНСТРУМЕНТЫ ЗАПИСИ, три ID воркфлоу) разобран по объекту (грeп CORE на диске сессии 2026-09-02): все три ID присутствуют в Skills google-drive-docs §1 и n8n-workflow-registry; в CORE Разделе 2 — ни одного. Вердикт: ШУМ по T-063 подтверждён, потери содержимого нет (живо в двух Skills + замороженный CORE по адресу восстановления). Из инструментов записи в канон уехал только GDOCS_APPLY_FROM_FILE (T-109, `registry/tools.md`).
Остаётся открытым: конфликт с SBE §9 настоящий — §9 требует держать все ID в CORE Разделе 2, а ID воркфлоу там никогда не жили; норма описывает порядок, которого в системе нет. Это про Skill, не про триаж. Лечение — на обслуживании, один из двух путей: завести ID инструментов в Р.2 и сделать §9 правдой, либо сузить §9 до ID документов и признать n8n-workflow-registry носителем ID воркфлоу (ближе к фактическому устройству).

## Опыт пилотного переноса (2026-09-04)

Дефект переноса: раздел-источник 14.2 (8 отвергнутых альтернатив) при первом переносе (Н-03.09-11) исчез без следа — не попал ни в «переезжает», ни в «невзятое». Причина: инвентаризация разделов источника не была обязательным шагом до триажа. Обнаружено слепым экзаменом (Н-04.09-01), закрыто прямым чтением источника (Н-04.09-04, commit fd384035), смерджено в main (Н-04.09-07). Превентив закреплён в /meta/PROTOCOL_file-migration.md («Этап 0: инвентаризация и классификация») — обязателен перед любым будущим переносом файла.

Организационная ошибка (повторилась дважды подряд): наряды Н-04.09-04 и Н-04.09-05 не указывали явно требование «результат — в main», оба landed на feature-ветках, потребовался отдельный merge-наряд (Н-04.09-07). Урок: ТРЕБУЕТ/РАМКА наряда на git-запись обязаны явно называть целевую ветку.

Архитектурный пробел (обнаружен 2026-09-04, сессия Cowork): работа над проектом вне Claude Project не имеет автоматической привязки к CORE/INDEX — Правило Феникса там не срабатывает, а «обслуживание» не может обнаружить отложенную регистрацию, потому что не сканирует внешние чаты и не хранит список таких сессий. Закреплено новым правилом §5.20 Skill second-brain-engineer: такая сессия обязана завершить write-back сама, до конца сессии, не откладывая на обслуживание.

Регистрация в CORE: git_migration сознательно не заведён в CORE Раздел 1/2 (проверено 2026-09-04 — полный список 14 проектов Раздела 1 прочитан целиком, совпадений нет) — решение Андрея: проект живёт как git-native, вне реестра CORE. Список «остальных проектов на перенос» в файлах этого проекта не найден — открытый вопрос.

Переименование: PLAN.md → Git_Migration_Plan.md выполнено Андреем напрямую через GitHub (commit f2b4888, 2026-09-04T15:12:24Z) — blob не изменился (sha fa40a7da8802161e1f73c782c2aea85d6cb92aaf сохранился), содержимое идентично.
