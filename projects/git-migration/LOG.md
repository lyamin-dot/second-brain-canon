projects/git-migration/LOG.md

Старые записи — `projects/git-migration/LOG_ARCHIVE.md`. Правило «номера, которого нет в этом файле, лежит в архиве» действует без исключений (§5.4 second-brain-git); безномерный блок («Хроника…», «Текст-кандидат…», «Продолжение…», «Наблюдения…») лежит в том же файле, что и запись, к которой он относится. В архиве на 2026-09-17: Р-01…Р-94, Т-01…Т-38 и Т-65…Т-71; номеров Т-39…Т-64 не существует — до Р-65 решения и тупики шли общим счётчиком (§5.4). Состав архива устанавливается чтением архива, а не историей коммитов.

Разрешение ссылок: номерное пространство Р-NN/Т-NN сквозное для журнала и архива, номера не переиспользуются. Внешняя ссылка вида «Р-NN журнала LOG.md» (§1 second-brain-git) разрешается через эту строку и при ротации не правится.

Ротациями 2026-09-16 туда уехали: наряд Н-16.09-04 (признаки 2а и 3 §5.2 second-brain-git) — Р-65…Р-76 и Т-67…Т-70 со своими безномерными блоками; наряд Н-16.09-08 (признаки 2а и 3) — Р-77…Р-84.

Ротацией 2026-09-17 (наряд Н-17.09-01, признаки 2, 2а и 4 §5.2 second-brain-git) туда уехали: Т-71 (тупик, условие возврата наступило и отработано — форма приземления Раздела 12 CORE решена и исполнена Р-94); Р-89, Р-90 (признак 2а — цитируются как основание в `core/R-11.md:35`); Р-94 (признак 2а — цитируется как основание в `core/MANIFEST.md:11`); Р-85…Р-88, Р-91…Р-93 (признак 4 — самые старые записи подряд от начала файла).

Ротацией 2026-09-18 (маршрут §9.4(б), Claude Code, `git push --dry-run` прошёл в этой сессии) туда уехали: Т-73 (признак 2 — тупик, условие возврата наступило и отработано, закрыт Р-104 записью о найденном владельцем credential `git-channel-webhook-auth`); Р-95, Р-96, Р-97, Р-98, Р-99, Т-72, Р-100, Р-101, Р-102 (признак 4 — самые старые записи подряд от начала файла). Ни один из этой десятки не назван основанием в PLAN.md проекта, в файле core/ или в Skill — проверено grep по origin/main. Первый push этой ротации был отбит («fetch first»): параллельная сессия дописала Т-74 между чтением и записью; расхождение не пересекалось построчно с уезжающими записями (§8.5), поэтому ротация пересобрана поверх актуальной версии без вопроса владельцу.

Р-103 | В СИЛЕ | 2026-09-18 | считать ли редакцию контракта 2026-09-18 принятой
  принято: редакция second-brain-git от 2026-09-18 в силе; позиции Z-1…Z-6 очереди закрыты. Владелец внёс новый текст Skill целиком, приёмка сделана по объекту: независимая сессия прочитала живой Skill и поштучно подтвердила девять признаков редакции — таблицу выбора транспорта в 7.6 с колонками по среде, карту узлов с Git Log и Git Write среди грязных, команду про includeData, переписанный 8.1 с таблицей доказательства записи, цену Git Log в 4б.8, правило гигиены имён тестовых файлов в 9.3, два новых запрета в 14. Ни одного номера исполнения n8n и ни одного упоминания Git Grep в тексте контракта не осталось.
  почему: правка Skill по слову владельца до сих пор помечалась в этом проекте как неподтверждённая машинно (позиции U-1, U-2), и только Р-97 показал, что синхронизированная копия Skill в среде сессии иногда обновляется в ту же сессию. Здесь это использовано штатно: не «владелец сказал, что внёс», а «прочитано и сверено по девяти признакам».
  кто подтвердил: Андрей, в чате 2026-09-18: «Новый скилл залил… я со всем согласен. И я согласен со всеми остальными правками».
  отвергнуто: закрывать позиции по слову владельца без чтения живого Skill — отвергнуто, потому что именно эта дешёвая проверка поймала дефект, описанный ниже.
  обратимость: дёшево — прежняя редакция 2026-09-17 восстанавливается из истории чата и из архива журнала; объект репозитория правка Skill не трогает.
  вывод сессии, проверяемый арифметикой: приёмка нашла дефект самой редакции — шесть перекрёстных ссылок внутри контракта вели на 7.1 вместо 7.6, след промежуточной версии, где таблица выбора стояла в 7.1, а потом была возвращена в 7.6 ради сохранности внешних ссылок. Дефект не виден чтением по диагонали и не ловится поиском битых ссылок: все шесть указывали на СУЩЕСТВУЮЩИЙ пункт, просто не на тот. Арифметика проверки: в тексте 96 номерованных пунктов и 21 уникальная внутренняя ссылка; разность множеств пуста, то есть ссылок в никуда нет, и именно поэтому автоматическая проверка на битые ссылки дала бы зелёный ответ на дефектном файле. Дефект мой, не владельца; исправленный файл отдан в тот же день.
  следствие для правила: любая правка, двигающая номер пункта, обязана сопровождаться пересчётом ссылок на этот номер по всему файлу. В этой сессии номер двигался дважды — сначала я сдвинул нумерацию §7, потом откатил, — и оба раза ссылки остались от промежуточного состояния.

Р-104 | В СИЛЕ | 2026-09-18 | закрыт ли тупик Т-73 о привязке credential к вебхукам канала
  принято: Т-73 закрыт, условие возврата наступило и отработано. Владелец завёл ОТДЕЛЬНЫЙ credential `git-channel-webhook-auth` типа Header Auth с именем заголовка `Authorization` и поставил его на все восемь вебхуков канала. Ключ не совпадает ни с токеном Wildberries, который подставляла автопривязка, ни с `github-canon-token`, которым узлы httpRequest представляются GitHub. Строка долга в registry/tools.md заменена описанием закрытого состояния и правилом на будущее.
  почему: условие возврата, записанное в Т-73, было сформулировано как «выбрать тот же credential, что у git-write» и к моменту исполнения оказалось неверным: у git-write с 2026-09-16 стоял тот же самый чужой токен Wildberries, то есть промах автопривязки был не одиночным, а повторился трижды и два раза остался незамеченным. Владелец закрыл не то, что было написано в условии, а то, что было нужно, — и это правильный исход, но он означает, что условие возврата тупика может устареть между записью и исполнением.
  кто подтвердил: Андрей, в чате 2026-09-18, предъявив credential скриншотом: `git-channel-webhook-auth`, Header Auth, Name = Authorization.
  отвергнуто: ставить на вебхуки `github-canon-token` — отвергнуто, хотя тип подходит и выбрать его можно. Это сделало бы GitHub-токен паролем от вебхука, и его утечка через вебхук давала бы не чтение репозитория, а прямую запись в него.
  обратимость: дёшево — credential отвязывается в UI, аутентификация снимается тем же update_workflow без параметра authentication.
  вывод сессии, проверяемый внутри записи: у Header Auth два противоположных смысла в зависимости от узла, и именно на этом построен весь класс ошибки. На триггере-вебхуке Value — то, что звонящий обязан предъявить. На HTTP Request-узле Value — то, что узел отправляет наружу. Тип credential один, направление разное, выпадающий список в UI их не различает. Отсюда и промах автопривязки: она выбирает по типу, а тип здесь ничего не говорит о назначении.
  допущение: что на всех восьми вебхуках стоит именно этот ключ, известно со слов владельца и подтверждено скриншотом одного credential. Проверить привязку по объекту нельзя: get_workflow_details поле credentials вырезает у всех восьми воркфлоу. Единственное, что подтверждено чтением объекта, — что на всех восьми стоит authentication=headerAuth.

Т-74 | 2026-09-18 | собрать содержимое labs/sys/INDEX.md для Git Commit Files ручным JSON-экранированием вместо программного дампа файла с диска
  почему не вышло: тело вызова Git Commit Files для labs/sys/INDEX.md было набрано вручную по уже готовому эталон-файлу на диске, посимвольно переэкранированному в \uXXXX внутри самого вызова, а не выведено скриптом из файла. Один вызов Git Commit Files (коммит 0af9baf, вместе с задачей t-repo-search-unknown-path.md) вернул success без единой ошибки, но потерял ровно одно предложение из середины первого абзаца INDEX.md — «Потолок 4б.3 снят 2026-09-17 решением по наряду Н-17.09-02 (Р-99); размеры файлов в списке не ведутся. Снятое и его адреса — projects/git-migration/NOT_TAKEN.md, раздел «Зона А SYS_INDEX»» — без изменения остального текста файла ни на байт. Файл задачи в том же вызове (короче, набран и передан тем же способом) дошёл байт в байт; расхождение — не в общем размере тела вызова, а в самой перепечатке этого конкретного файла.
  условие возврата: правило 8.2 стоит читать буквально дальше его явного текста: «браться из файла, собранного на диске» относится не только к тому, что текст сочиняется заранее и лежит в файле, но и к тому, как он попадает в тело вызова: выводом скрипта (json.dumps файла), не ручной повторной перепечаткой уже готового файла в параметры вызова. Ручная перепечатка проверена этой сессией как источник потери без сигнала об ошибке — независимая проверка 8.1 (diff записанного с эталоном, а не success ответа) поймала расхождение и позволила исправить до отчёта владельцу. Не зафиксировано отдельным правилом Skill — только этой записью; вопрос владельцу: заводить ли в second-brain-git явный пункт «content для Git Commit Files передаётся выводом скрипта, не ручным набором» рядом с 8.2, или считать записи 8.1/8.2 достаточными.
  стоимость попытки: один лишний вызов Git Replace на восстановление (коммит 1c94567); потеря обнаружена и закрыта в той же сессии, до того как была названа владельцу как факт завершённой записи.

Р-105 | В СИЛЕ | 2026-09-18 | позиция X-1 закрыта: ротация LOG.md исполнена
  принято: `git push --dry-run` прошёл (ветка и `main`) — §9.4(б) исполним, чего не было у X-1. Журнал собран скриптом нарезкой из origin/main, sha проверен до записи, закоммичен и запушен fast-forward без merge и PR. Первый push отбит («fetch first», параллельная сессия дописала Т-74); diff не пересёкся с уезжающими (§8.5), пересобрано поверх актуальной (`98e6c58`).
  почему: §9.1 — предикат проверяется заново в каждой сессии.
  кто подтвердил: Андрей, «Прими эстафету…».
  приёмка §5.3 на origin/main: sha совпали с расчётными (`LOG.md` fa200648, архив 6413c05d); старый архив (353 748) — точный префикс нового (389 800); номера Р-01…Р-104, Т-01…Т-38, Т-65…Т-74 (152, разрыв Т-39…Т-64 по §5.4) — все, без потерь и пересечений. Уехало десять: Т-73, Р-95…Р-99, Т-72, Р-100…Р-102.