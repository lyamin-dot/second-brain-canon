projects/kontext-reduktion/LOG.md

Журнал решений и тупиков проекта. Нумерация Р-NN и Т-NN внутри этого файла. Пишется и принятое, и отвергнутое — поле «отвергнуто» у решения обязательно, если альтернативы рассматривались.

Р-1 | В СИЛЕ | 2026-09-24 | где вести работу по уменьшению обязательного чтения
  принято: отдельная летопись `projects/kontext-reduktion/` с ролями STATE, PLAN, LOG по §4 second-brain-git.
  почему: у темы есть финиш (PLAN.md, раздел «Финиш»), значит это летопись, а не лаборатория (§4б.1); решения уже шли пачками и рассыпались по журналу git-migration и очереди.
  отвергнуто: 2 — (а) задача в `labs/sys/`: у задачи лаборатории нет фаз и общего следующего шага, а здесь они есть (§4б.7); (б) продолжать в журнале git-migration: у того проекта другой финиш, и его журнал уже над бюджетом чтения.
  кто подтвердил: Андрей, в чате SYS 2026-09-24: «пора создать летопись этого проекта… назвать kontext-reduktion».
  обратимость: дёшево — закрытие проекта есть смена статуса в `registry/projects.md`.

Р-2 | В СИЛЕ | 2026-09-24 | две таблицы в Манифесте CORE: «Список» и «По событию»
  принято: решение записано как Р-111 журнала `projects/git-migration/LOG.md` до заведения этой летописи; здесь — его отвергнутые варианты, потому что летопись собирает отвергнутое.
  отвергнуто: 4 — (а) раскрывать список CORE внутри воркфлоу Git Read Batch: воркфлоу стал бы зависеть от формата markdown-таблицы, агент перестал бы видеть таблицу «По событию», пришлось бы править мастер-промпты всех проектов; (б) полностью ленивый CORE: первая запись в сессии прошла бы без контракта записи; (в) только перенос `core/R-06c.md` в Skill: около 12 процентов экономии, Манифест не тронут; (г) удалить летопись редакций Манифеста целиком: нарушило бы п.0.4.1 `core/R-06a.md` — опровергнутые признаки чтения оставлены строками ИСТОРИЯ ДИАГНОЗА.
  кто подтвердил: Андрей, 2026-09-24, «Даю добро на выполнение плана» после ревизии denken Р3.

Р-3 | В СИЛЕ | 2026-09-24 | что ещё снять со старта CORE
  принято: (1) `core/R-08.md` и `core/R-11.md` — в таблицу «По событию», строка записи в INDEX или журнал; (2) `core/R-06a.md` разрезан по теме: блоки T-047 и T-049 (адресация записи, ШАГ 0.5 и маршрутная таблица 0.5.3) — в новый `core/R-06-marshrut.md`, в R-06a остались T-044…T-046 (базовый контракт записи, ШАГ 0, п.0.4 и 0.4.1); (3) `meta/header.md` снят со старта. «Список» — 4 файла: R-04, R-05, R-06a, R-07.
  почему: критерий состава Манифеста — в «Список» только то, что нужно любой сессии в момент чтения CORE. R-08 (шапка метаданных, формат INBOX), R-11 (LAST_UPDATED, перенос закрытой задачи в архив) и адресация записи нужны только при записи. Оба поля `meta/header.md` помечены недействующими 2026-09-24 другой сессией; чтение шапки ничего не доказывает.
  отвергнуто: 3 — (а) оставить R-08 и R-11 на старте: выбор по размеру, а не по критерию, — такой выбор ревизия denken Р3 назвала дефектом плана; (б) пересобрать весь Раздел 6 с переименованием R-06b, R-06c, R-06d в тематические имена: правка без выигрыша чтения, а R-06c уходит из `core/` по Q-4; (в) удалить `meta/header.md` из репозитория: провенанс-маркер T-001 называет его строки составом блока и стал бы ложным.
  отступление от рамки: тело блоков T-047 и T-049 прошло через модель в вызове Git Commit Files, хотя §4.5 и §8.3 second-brain-git требуют для уже существующего текста маршрут 9.4(б). В этой среде push отказан. Мера против класса Р-79 git-migration: blob sha обоих файлов посчитан скриптом до коммита из байт исходного файла и сверен после; сумма байт 4 168 + 2 007 − 22 (строка пути) = 6 153, каждый T-NN ровно в одном файле (§6.8).
  кто подтвердил: Андрей, 2026-09-24, «Выполни свои предложения».
  обратимость: дёшево — строки таблиц Манифеста; прежний R-06a читается по ref родителя коммита.
  вывод сессии, проверяемый арифметикой: старт SYS = core/MANIFEST.md 7 599 + labs/sys/INDEX.md 5 313 + R-04 1 080 + R-05 1 260 + R-06a 4 168 + R-07 1 726 = 21 146 Б. Утром того же дня — 46 415 Б.

Р-4 | В СИЛЕ | 2026-09-24 | разбор `projects/git-migration/STATE.md` (позиция Q-5 очереди)
  принято: разбор по §5.1: закрытое с номером решения — указателем на номер, снимки чисел удалены, живое оставлено. 18 530 Б → 8 855 Б при бюджете чтения 8 192 Б. Записано одним Git Write с blob sha, посчитанным до записи.
  почему: STATE читается на старте каждой сессии git-migration; две трети файла были закрытыми пунктами, у каждого уже есть запись Р-NN в журнале или архиве.
  отвергнуто: 3 — (а) поднять бюджет STATE: файл тяжёл из-за закрытого, а не из-за тесноты живого; (б) вынести закрытое хроникой в LOG.md по §5.5 п.2: у каждого пункта уже есть решение в журнале — вышел бы дубль, а журнал и так над бюджетом (AF-1); (в) серия из одиннадцати Git Replace: через модель прошёл бы тот же объём удаляемого текста в oldStr, но одиннадцатью коммитами; один Git Write со сверкой заранее посчитанного sha проверяется так же строго.
  кто подтвердил: Андрей, 2026-09-24, «Выполни свои предложения».
  допущение: превышение на 663 Б остаётся; следующий разбор — когда закроется ф4.

Р-5 | В СИЛЕ | 2026-09-24 | сверка byteLength с текстом при чтении через Канал
  принято: сверка снята из Skills second-brain-git §7.1, second-brain-engineer (блок !save) и предложена к снятию из мастер-промпта SYS (`labs/sys/t-master-prompt-sys.md`).
  почему: узел 04_Build_Response у Git Read и Git Read Batch считает byteLength как Buffer.from(base64).length — из тех же байт, что и text; сверка сравнивает файл сам с собой. Агент к тому же не может пересчитать байты UTF-8 у десятков килобайт без второй вставки текста в контекст.
  отвергнуто: 1 — оставить сверку: условие невыполнимо и молча пропускалось, то есть давало ложную уверенность.
  отложено, не отвергнуто: сверка в узле 04_Build_Response длины буфера с полем size ответа GitHub — отдельным нарядом, если понадобится защита от обрезки.
  кто подтвердил: Андрей, 2026-09-24.

Р-6 | В СИЛЕ | 2026-09-24 | форма входа Git Commit Files в реестре
  принято: в `registry/tools.md` к строке git-commit дописана форма элемента `files[]` — `{path, content, mode?}` — и правило: сигнатуру брать из реестра, `get_workflow_details` звать только когда её там нет.
  почему: формы элемента в реестре не было; сессия SYS 2026-09-24 вызвала `get_workflow_details` ради одной строки — ответ около 25 КБ, воркфлоу в нём дважды (рабочая и опубликованная версия).
  кто подтвердил: Андрей, 2026-09-24, «Выполни свои предложения».

Р-7 | В СИЛЕ | 2026-09-24 | Skills с пометкой «ОБЯЗАТЕЛЬНО»
  принято: направление — обязательная часть SKILL.md до 15 КБ, разовые процедуры в references/ по ссылке; первым `second-brain-git`. Вслед за этим сузить триггеры: чтение файла не тянет правила записи и ротации. Исполнение отложено.
  почему: после Р-3 обязательное чтение держат Skills: second-brain-git 71 КБ, n8n-engineering-manual 73 КБ, google-drive-docs 68 КБ, naryad 59 КБ, second-brain-engineer 52 КБ. Сессия, тронувшая repo, читает `second-brain-git` целиком — втрое больше всего старта CORE.
  отвергнуто: 1 — сузить триггеры без разделения: сессия, читающая файл, потеряла бы таблицу выбора транспорта §7.6.
  кто подтвердил: Андрей, 2026-09-24: «это тоже нужно сделать, только потом».
  цена: Skill из нескольких файлов заливается только zip, кнопка Save skill не подходит (google-drive-docs §12).

Р-8 | В СИЛЕ | 2026-09-24 | идеи, проверенные и отвергнутые
  отвергнуто: 3 — (а) удалить серии пустых строк в файлах CORE: замер на 10 файлах — около 380 Б, правка не окупается; (б) сжать формулировки: ужимание, запрещено §5.1; (в) держать копию CORE в документах Claude Project, чтобы не читать Каналом: второй носитель канона, нарушает И-1.
  кто подтвердил: вывод сессии; все три названы владельцу в чате SYS 2026-09-24 как отвергнутые, его ответ «Выполни свои предложения» касался остальных пунктов.


Р-9 | В СИЛЕ | 2026-09-24 | приёмка работы 2026-09-24 независимым Контролёром
  принято: вердикт Контролёра «ПРИНЯТО С ЗАМЕЧАНИЯМИ». Содержание не потеряно: разрез R-06a побайтово совпал со старым, каждый удалённый из STATE git-migration фрагмент найден в журнале, архиве, карте, очереди или NOT_TAKEN.md; арифметика Р-3 и Р-4 сошлась. Исправлено той же сессией: (1) Р-5 и `labs/sys/t-master-prompt-sys.md` утверждали, что сверка byteLength «снята из Skills», — на деле правка выдана владельцу и ждёт заливки; до заливки действует строка 7 `meta/PENDING_RULES.md`; (2) шапка `labs/sys/INDEX.md` называла SYS_INDEX замороженным — теперь «заморозка решена Р-109, исполнение нарядом Н-23.09-05 в репозитории не записано»; (3) STATE git-migration снова называет позицию U-1, возвращённую в открытые в 10:46, и ссылается на Р-4 этого журнала; (4) в `meta/MAP_core-sections.md` сняты неверные размеры Р.6 (R-06b на 2026-09-10 весил 3 613 Б, не 2 374); (5) Манифест больше не утверждает, что любой сдвиг состава обходится без правки мастер-промптов; (6) событие «запись в INBOX» добавлено в таблицу «По событию» — формат INBOX лежит в R-08; (7) STATE этого проекта: 21 146 Б — после правки 4 мастер-промпта, до неё старт 22 727 Б.
  поправки к записям выше (записи не редактируются, 11.1): Р-3 — blob sha по §6.8: `core/R-06a.md` ff6212dd, `core/R-06-marshrut.md` ad1b32c6; Р-4 — закрытое составляло 52 процента файла (9 675 из 18 530 Б), а не две трети; Р-7 — размеры Skills названы по отредактированным копиям, живые на 2026-09-24: second-brain-git 69 569 Б, google-drive-docs 61 967 Б.
  не исправлено, за владельцем: итог приёмки ф3 нигде не записан, а от него зависит старт ф6; маркера T-048 нет за всю историю, а карта пишет диапазон T-044…T-051.
  кто подтвердил: запуск предложен владельцем вопросом «Нужно ли запустить Контролёра, который проверит достигнутый результат?» 2026-09-24; запущен сессией, вердикт принят сессией.
  отвергнуто: 1 — проверять только байты: побайтовая сверка уже была доказана sha до вопроса владельца; ценность Контролёра — в смысле удалённого и в правдивости утверждений, и именно там нашлись три дефекта.

Р-10 | В СИЛЕ | 2026-09-24 | фрагментированное чтение INDEX-файлов и файлов летописи
  принято: вывод исследования (агент-исследователь по заданию владельца «отрицательный результат меня так же устроит»), внедрение ждёт решения владельца. Фрагментировать чтение окупается только для LOG.md: сессия, берущаяся за проект, читает оглавление журнала одним поиском Git Read Lines по шаблону `^(Р|Т)-[0-9]+ \|` и затем только нужные записи по номеру. STATE.md, PLAN.md и INDEX.md лаборатории не фрагментируются.
  почему: замер по клону на коммите b3878a9. Журнал git-migration производит в среднем 21,7 КБ записей в рабочий день (медиана 16,8, пик 66) — больше своего бюджета чтения 16 384 Б, поэтому правило «читать целиком» держалось только на 25 ротациях за 13 дней. Строки-заголовки записей — 4,8 процента журнала, их уже требует форма записи §11, вести оглавление отдельно не нужно. Взятие проекта git-migration: около 34,1 КБ сейчас против около 13,3 КБ плюс открытые записи. В STATE git-migration живое состояние и следующий шаг разбросаны по всем разделам: чтение одного-двух разделов теряет 43 процента живого, экономя около 3,8 КБ. INDEX лаборатории уже устроен как список плюс тела задач; отдельное чтение его строк стоит на 25 Б дороже чтения внутри пакета старта. PLAN почти не растёт; перерос бюджет только PLAN n8n-map (41 798 Б), и то из-за доказательств, место которым по §4.2 в LOG.
  отвергнуто: 12 — (1) оглавление, которое ведут вручную: ручная синхронизация при каждой записи и второй носитель заголовков, а то же оглавление бесплатно выводится поиском; (2) голова файла, первые N строк: строка — абзац переменной длины, журнал растёт вниз и голова отдаёт старое, N — магическое число; (3) разнесение STATE на несколько файлов: пятая роль (4.1), а по 4.5 читателю безразлична раскладка — экономии ноль; (4) файл на каждую запись журнала плюс список: уступает поиску по чтению, дублирует заголовок, переводит запись с Git Append на воркфлоу с утечкой данных исполнения — оставлен в резерве; (5) STATE с чтением только «Следующего шага»: теряет 43 процента живого; (6) журнал только по номеру без оглавления: сессия не знает номеров вне STATE и PLAN — вошёл в принятый вариант второй половиной; (7) фильтр по метке или статусу: старт должен видеть все задачи, а отменённых записей в журнале 1 054 Б из 369 199; (8) фрагментное чтение INDEX лаборатории любым способом: экономия отрицательная; (9) дайджест журнала, который пишут сессии: второй носитель; (10) копия в документах Claude Project: второй носитель, уже отвергнуто Р-8; (11) gdocs-read с диапазоном строк: не экономит контекст, к репозиторию не относится; (12) отменить ротацию совсем: без доработки Git Read Lines оглавление неограниченного журнала растёт само, а файл идёт к пределу GitHub.
  отложено, не отвергнуто: параметры `until` и `tail` для Git Read Lines — чтение раздела до стоп-шаблона одним вызовом и оглавление постоянного размера; нужны правка воркфлоу и зонд до правила (И-3).
  слабое звено: оглавление полезно настолько, насколько поле «вопрос» в строке-заголовке говорит о сути записи; смягчение уже есть — решения, названные в STATE и PLAN, открываются по номеру (на объекте 5 из 6 записей журнала git-migration названы в STATE).
  найдено попутно, кандидат в БЛОКЕРы, решение за владельцем: `projects/git-migration/LOG_ARCHIVE.md` весит 405 129 Б; GitHub отдаёт поле content пустым для файлов больше 1 МБ, а узел github в n8n берёт только content — гипотеза: Git Read по такому файлу вернёт пустой текст без ошибки. При нынешнем темпе предел — примерно через 28 рабочих дней. Зондом не проверено.
  кто подтвердил: вывод исследования; владелец ставил задачу и принимает решение о внедрении.

Р-11 | В СИЛЕ | 2026-09-24 | внедрить чтение журнала LOG.md через оглавление
  принято: вывод Р-10 внедряется. В Skill second-brain-git: таблица §4, строка LOG.md — сессия, берущаяся за проект, читает оглавление журнала и затем только нужные записи по номеру, файл целиком не читается; таблица 7.6 — две строки «оглавление журнала» и «запись журнала по номеру», шаблоны вынесены из таблицы в абзац под ней, потому что знак | внутри ячейки ломает таблицу; §5.2 — ротация наступает, когда выше бюджета чтения выходит оглавление, а не весь файл, и обязательна до размера 1 МБ, после которого GitHub не отдаёт содержимое файла через Contents API; таблица цен 7.6 — у строки «только заголовки записей» названо основание: 0,015× без конверта ответа, около 0,06× с конвертом. До заливки Skill владельцем правило действует строкой 8 `meta/PENDING_RULES.md`.
  почему: Р-10 — журнал растёт быстрее своего бюджета чтения, оглавление бесплатно выводится из формы записи §11 и весит около 5 процентов журнала.
  отвергнуто: 1 — держать прежнее условие ротации «весь файл выше бюджета»: при чтении оглавлением оно заставляло бы ротировать журнал почти ежедневно без выгоды для читателя.
  кто подтвердил: Андрей, в чате SYS 2026-09-24: «Внедряем чтение журнала через оглавление».
  следствие: позиция AF-1 очереди (ротация журнала git-migration) теряет основание после заливки Skill — оглавление журнала git-migration около 1,3 КБ.

Р-12 | В СИЛЕ | 2026-09-24 | нарезка Skills на обязательную часть и справку (рычаг к3)
  принято: second-brain-git и google-drive-docs нарезаны скриптом: существующий текст перенесён по строкам без перепечатки, проверено — каждая строка исходника попала ровно в один файл. Сессия написала только связки: таблицу «Что читать дальше» в начале SKILL.md, указатели на вынесенные пункты и правило Р-11. Номера пунктов сохранены, поэтому ссылки вида «second-brain-git 8.3» и «google-drive-docs §3.2» остаются верными. second-brain-git: 70 991 Б в одном файле → SKILL.md 23 906 Б плюс references/ write 21 064, delivery 8 980, lab 8 092, costs 5 407, rotation 5 293, migration 4 877, core-split 3 080. google-drive-docs: 68 074 Б → SKILL.md 18 005 Б плюс references/ failures 14 435, docs-write 10 823, mcp 10 581, sheets 9 103, docs-read 5 099, infra 3 362. Сессия, которая только читает repo, платит 24 КБ вместо 71; сессия, которая пишет, — около 45 КБ.
  отвергнуто: 3 — (а) сжать формулировки обязательной части до 15 КБ сейчас: это переписывание правил, а не нарезка, с риском потерять смысл; делается отдельным шагом с приёмкой Контролёра; (б) нарезать second-brain-engineer, naryad и n8n-engineering-manual этой же сессией: их полный текст сессия не читала, а решать, что нужно всегда, по одним заголовкам нельзя; (в) сузить триггеры в описании Skill: таблица «Что читать дальше» даёт тот же выигрыш, не рискуя, что Skill вообще не будет прочитан.
  цена: Skill из нескольких файлов заливается только zip-архивом через Customize → Skills, кнопка Save skill не подходит (google-drive-docs §12). Канонический источник google-drive-docs — нативный Google Doc: он не хранит несколько файлов, поэтому источник либо повторяет разбивку разделами, либо перестаёт быть каноническим — решение за владельцем.
  кто подтвердил: Андрей, в чате SYS 2026-09-24: «У тебя хватит контекстного окна нарезать Skill? Если хватит, сделай».
  финиш PLAN.md п.2 (обязательная часть до 15 КБ) не достигнут: 23,9 и 18,0 КБ.

Р-13 | В СИЛЕ | 2026-09-24 | принципиально иные способы уменьшить стоимость чтения, кроме нарезки и таблиц CORE
  принято: перечень кандидатов записан в PLAN.md рычагами к8–к13, решение по каждому — за владельцем. к8 — чтение через посредника: тяжёлое чтение делает субагент и возвращает только ответ на вопрос, а не текст; к9 — узкие читатели на стороне n8n: воркфлоу, отдающий только сигнатуру входа другого воркфлоу, и чтение раздела markdown по заголовку; к10 — тонкий мастер-промпт: роль, обязательный формат анализа и режим старшего инженера уходят в Skill, в промпте остаются старт и ссылки; к11 — запись фрагментами: тело каждого вызова записи остаётся в контексте сессии до её конца; к12 — ответ короче прочитанного: развёрнутое уходит в файл, в чат — итог; к13 — правила в виде таблиц решений вместо прозы, путь к 15 КБ обязательной части Skill.
  почему: сегодняшний замер сессии SYS — самое дорогое в контексте не CORE (21 КБ), а Skills (70 КБ на один только second-brain-git до нарезки), ответы get_workflow_details (около 25 КБ каждый, три вызова), тела вызовов записи (Т-1) и длинные ответы в чат. Контролёр и исследователь как субагенты прочитали по 200 тысяч токенов каждый, а в контекст сессии вернули около 10 КБ отчёта на двоих — это и есть к8 на деле.
  отвергнуто: 4 — (а) чтение разницы с прошлой сессией: у новой сессии нет копии прошлого текста, к которой применять разницу, а память Claude как носитель запрещена принципом 3 мастер-промпта; (б) CORE прямо в мастер-промпте или в документах Claude Project: второй носитель канона (И-1); (в) поиск по смыслу (векторный индекс) по правилам: пропущенное правило не видно, а для контракта записи пропуск недопустим — допустим только для поиска по архиву журнала; (г) сокращения и аббревиатуры в тексте правил: владелец их запрещает, а экономия съедается ошибками понимания.
  кто подтвердил: вывод сессии по вопросу владельца «Есть ли ещё принципиально иные методы уменьшить стоимость чтения» 2026-09-24; внедрение — по слову владельца.

Т-1 | 2026-09-24 | запись в репозиторий целыми файлами через Git Write и Git Commit Files в сессии без клона с push
  почему не вышло: тело вызова остаётся в контексте сессии до её конца, а харнесс сериализует кириллицу во входе инструмента экранированными последовательностями \uXXXX — замер на projects/kontext-reduktion/LOG.md: 17 061 символ текста дают 69 498 символов экранированного JSON, в 4,05 раза больше; попытка писать сырой кириллицей на это не повлияла. Оценка по тому же множителю: один вызов Git Commit Files этой сессии с пятью файлами летописи (около 21 тысячи символов текста) занял в контексте около 85 тысяч символов.
  условие возврата: среда, где `git push --dry-run` проходит (запись идёт из файла, тело не проходит через вызов инструмента), либо воркфлоу записи, берущий тело из файла на Drive по образцу gdocs-apply-from-file.
  стоимость попытки: заметная доля контекста сессии; вывод — рычаг к11 PLAN.md.

Р-14 | ОТМЕНЕНО Р-15 | 2026-09-24 | откуда множитель 4,05 в Т-1 и сколько стоит кириллица в теле вызова инструмента
  принято: вывод замера — множитель Т-1 настоящий, но его даёт набор кириллицы в теле вызова последовательностями \uXXXX, а не харнесс. Кириллица, набранная в теле вызова как есть, стоит примерно втрое меньше. К такому набору подталкивает пункт 8.2 second-brain-git («текст передаётся JSON-экранированным»): python json.dumps по умолчанию (ensure_ascii=True) заменяет каждую букву кириллицы шестью символами \uXXXX. Правка пункта 8.2 — позиция AG-3 очереди.
  почему: замер в облачной сессии Claude Code 2026-09-24 по полю usage журнала сессии, в токенах, а не в символах. Один абзац (Р-5 этого журнала, 576 символов, 909 Б) записан инструментом Write трижды, каждый вызов отдельным ходом. По-английски — 339 токенов выхода; русский как есть — не больше 539 (в число вошёл короткий блок рассуждения модели перед вызовом); русский с \uXXXX, набранными моделью, — 1 560. Прирост контекста на следующем ходу превышал выход вызова на 104–106 токенов во всех трёх случаях: тело хранится в контексте в том виде, в каком модель его набрала, харнесс его не раздувает. Файл, записанный из \uXXXX, вышел на диске побайтово равным русскому (576 символов, 909 Б): экранирование снимается при разборе JSON и оплачивается только моделью, которая его набирает. Т-1 утверждал, что попытка писать сырой кириллицей на множитель не повлияла, — этот замер утверждение опровергает.
  вывод сессии, проверяемый арифметикой: (1) 1 560 / 539 = 2,9 — нижняя граница множителя по полному вызову, без служебной части вызова (путь к файлу) он выше трёх; (2) число Т-1 совпадает с json.dumps: 17 061 + 5 × 10 487 = 69 498, то есть ровно замена каждой кириллической буквы шестью символами при доле кириллицы 61,5 процента; доля кириллицы в этом журнале на 2026-09-24 — 61,7 процента (12 343 из 20 003 символов), json.dumps с ensure_ascii=True даёт на нём 4,14, с ensure_ascii=False — 1,0.
  попутно: русский дороже английского на этом абзаце примерно на 30 процентов (не больше 539 против 339 токенов; в абзаце много латинских имён).
  отвергнуто: 2, сессией — (а) запись фрагментами (рычаг к11 PLAN.md) как лекарство от Т-1: уменьшает объём тела, но не способ набора, и фрагмент в \uXXXX стоит так же втрое дороже; (б) перевести на английский правила, которые читает агент: около 30 процентов экономии ценой второго носителя (И-1).
  допущение: замер снят в облачной сессии Claude Code, а не в Claude Project; тело вызова в текст для модели переводит API, поэтому перенос вывода на Claude Project вероятен, но зондом там не проверен.
  кто подтвердил: Андрей, 2026-09-24, «Пиши» — на предложение записать замер и поправку к Т-1.
  обратимость: дёшево — новая запись отменяет эту.

Р-15 | В СИЛЕ | 2026-09-24 | отмена главного вывода Р-14: тело вызова раздувает харнесс, но только у параметра-объекта
  принято: Р-14 отменяется. Её вывод «множитель даёт набор модели, а не харнесс» для Канала неверен. Тело, переданное параметром-объектом (у инструмента MCP n8n execute_workflow это inputs), хранится в контексте последовательностями \uXXXX, как бы модель его ни набрала; параметр-строка верхнего уровня (content у Write) хранится так, как набран. Т-1 в главном верен, включая утверждение, что запись сырой кириллицей не помогает: модель на это не влияет. Позиция AG-3 очереди, названная в Р-14, не заведена: предложенная там правка пункта 8.2 Канал не удешевляет.
  почему: замер той же сессии на записи самой Р-14. Вызов Git Append через execute_workflow с телом 4 610 Б: выход модели 1 429 токенов, прирост контекста на следующем ходу 8 175 токенов при ответе инструмента в одну строку ({"executionId":"68331","status":"started"}). У трёх вызовов Write из Р-14 прирост превышал выход на 104–106 токенов. В журнале сессии тело этого вызова хранится обычной кириллицей, а в контексте модели тот же вызов виден последовательностями \uXXXX: экранирование добавляется при переводе вызова в текст для модели.
  вывод сессии, проверяемый арифметикой: 8 175 / 1 429 = 5,7 — множитель в токенах для тела с долей кириллицы около 60 процентов; Т-1 называл 4,05 в символах.
  допущение: модель видит свои прошлые вызовы экранированными и, вероятно, поэтому сама начинает набирать \uXXXX (Т-66 и Т-74 журнала git-migration, Т-75 там же) — не проверено.
  кандидат, решение за владельцем: тело кладётся на Drive инструментом, у которого содержимое — параметр-строка верхнего уровня, а воркфлоу записи получает только ID файла, по образцу gdocs-apply-from-file (второе условие возврата Т-1); сначала — зонд, что у этого инструмента тело в контексте не раздувается.
  кто подтвердил: Андрей, 2026-09-24, «Пиши»; отмена Р-14 — вывод сессии по замеру, снятому при исполнении этого слова.
  обратимость: дёшево — новая запись отменяет эту.