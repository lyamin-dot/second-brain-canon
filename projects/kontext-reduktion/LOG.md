projects/kontext-reduktion/LOG.md

Журнал решений и тупиков проекта. Нумерация Р-NN и Т-NN внутри этого файла. Пишется и принятое, и отвергнутое — поле «отвергнуто» у решения обязательно, если альтернативы рассматривались.

Р-1 | В СИЛЕ | 2026-09-24 | где вести работу по уменьшению обязательного чтения
  принято: отдельная летопись `projects/kontext-reduktion/` с ролями STATE, PLAN, LOG по §4 second-brain-git.
  почему: у темы есть финиш (PLAN.md, раздел «Финиш»), значит это летопись, а не лаборатория (§4б.1); решения уже шли пачками и рассыпались по журналу git-migration и очереди.
  отвергнуто: 2 — (а) задача в `labs/sys/`: у задачи лаборатории нет фаз и общего следующего шага, а здесь они есть (§4б.7); (б) продолжать в журнале git-migration: у того проекта другой финиш, и его журнал уже над бюджетом чтения.
  кто подтвердил: Андрей, в чате SYS 2026-09-24: «пора создать летопись этого проекта… назвать kontext-reduktion».
  обратимость: дёшево — закрытие проекта есть смена статуса в `registry/projects.md`.

Р-2 | В СИЛЕ | 2026-09-24 | две таблицы в Манифесте CORE: «Список» и «По событию»
  принято: решение записано как Р-111 журнала `projects/git-migration/LOG.md` до заведения этой летописи; здесь — его отвергнутые варианты, потому что летопись собирает отвергнутое.
  отвергнуто: 4 — (а) раскрывать список CORE внутри воркфлоу Git Read Batch: воркфлоу стал бы зависеть от формата markdown-таблицы, агент перестал бы видеть таблицу «По событию», пришлось бы править мастер-промпты всех проектов; (б) полностью ленивый CORE: первая запись в сессии прошла бы без контракта записи; (в) только перенос `core/R-06c.md` в Skill: около 12 процентов экономии, Манифест не тронут; (г) удалить летопись редакций Манифеста целиком: нарушило бы п.0.4.1 `core/R-06a.md` — опровергнутые признаки чтения оставлены строками ИСТОРИЯ ДИАГНОЗА.
  кто подтвердил: Андрей, 2026-09-24, «Даю добро на выполнение плана» после ревизии denken Р3.

Р-3 | В СИЛЕ | 2026-09-24 | что ещё снять со старта CORE
  принято: (1) `core/R-08.md` и `core/R-11.md` — в таблицу «По событию», строка записи в INDEX или журнал; (2) `core/R-06a.md` разрезан по теме: блоки T-047 и T-049 (адресация записи, ШАГ 0.5 и маршрутная таблица 0.5.3) — в новый `core/R-06-marshrut.md`, в R-06a остались T-044…T-046 (базовый контракт записи, ШАГ 0, п.0.4 и 0.4.1); (3) `meta/header.md` снят со старта. «Список» — 4 файла: R-04, R-05, R-06a, R-07.
  почему: критерий состава Манифеста — в «Список» только то, что нужно любой сессии в момент чтения CORE. R-08 (шапка метаданных, формат INBOX), R-11 (LAST_UPDATED, перенос закрытой задачи в архив) и адресация записи нужны только при записи. Оба поля `meta/header.md` помечены недействующими 2026-09-24 другой сессией; чтение шапки ничего не доказывает.
  отвергнуто: 3 — (а) оставить R-08 и R-11 на старте: выбор по размеру, а не по критерию, — такой выбор ревизия denken Р3 назвала дефектом плана; (б) пересобрать весь Раздел 6 с переименованием R-06b, R-06c, R-06d в тематические имена: правка без выигрыша чтения, а R-06c уходит из `core/` по Q-4; (в) удалить `meta/header.md` из репозитория: провенанс-маркер T-001 называет его строки составом блока и стал бы ложным.
  отступление от рамки: тело блоков T-047 и T-049 прошло через модель в вызове Git Commit Files, хотя §4.5 и §8.3 second-brain-git требуют для уже существующего текста маршрут 9.4(б). В этой среде push отказан. Мера против класса Р-79 git-migration: blob sha обоих файлов посчитан скриптом до коммита из байт исходного файла и сверен после; сумма байт 4 168 + 2 007 − 22 (строка пути) = 6 153, каждый T-NN ровно в одном файле (§6.8).
  кто подтвердил: Андрей, 2026-09-24, «Выполни свои предложения».
  обратимость: дёшево — строки таблиц Манифеста; прежний R-06a читается по ref родителя коммита.
  вывод сессии, проверяемый арифметикой: старт SYS = core/MANIFEST.md 7 599 + labs/sys/INDEX.md 5 313 + R-04 1 080 + R-05 1 260 + R-06a 4 168 + R-07 1 726 = 21 146 Б. Утром того же дня — 46 415 Б.

Р-4 | В СИЛЕ | 2026-09-24 | разбор `projects/git-migration/STATE.md` (позиция Q-5 очереди)
  принято: разбор по §5.1: закрытое с номером решения — указателем на номер, снимки чисел удалены, живое оставлено. 18 530 Б → 8 855 Б при бюджете чтения 8 192 Б. Записано одним Git Write с blob sha, посчитанным до записи.
  почему: STATE читается на старте каждой сессии git-migration; две трети файла были закрытыми пунктами, у каждого уже есть запись Р-NN в журнале или архиве.
  отвергнуто: 3 — (а) поднять бюджет STATE: файл тяжёл из-за закрытого, а не из-за тесноты живого; (б) вынести закрытое хроникой в LOG.md по §5.5 п.2: у каждого пункта уже есть решение в журнале — вышел бы дубль, а журнал и так над бюджетом (AF-1); (в) серия из одиннадцати Git Replace: через модель прошёл бы тот же объём удаляемого текста в oldStr, но одиннадцатью коммитами; один Git Write со сверкой заранее посчитанного sha проверяется так же строго.
  кто подтвердил: Андрей, 2026-09-24, «Выполни свои предложения».
  допущение: превышение на 663 Б остаётся; следующий разбор — когда закроется ф4.

Р-5 | В СИЛЕ | 2026-09-24 | сверка byteLength с текстом при чтении через Канал
  принято: сверка снята из Skills second-brain-git §7.1, second-brain-engineer (блок !save) и предложена к снятию из мастер-промпта SYS (`labs/sys/t-master-prompt-sys.md`).
  почему: узел 04_Build_Response у Git Read и Git Read Batch считает byteLength как Buffer.from(base64).length — из тех же байт, что и text; сверка сравнивает файл сам с собой. Агент к тому же не может пересчитать байты UTF-8 у десятков килобайт без второй вставки текста в контекст.
  отвергнуто: 1 — оставить сверку: условие невыполнимо и молча пропускалось, то есть давало ложную уверенность.
  отложено, не отвергнуто: сверка в узле 04_Build_Response длины буфера с полем size ответа GitHub — отдельным нарядом, если понадобится защита от обрезки.
  кто подтвердил: Андрей, 2026-09-24.

Р-6 | В СИЛЕ | 2026-09-24 | форма входа Git Commit Files в реестре
  принято: в `registry/tools.md` к строке git-commit дописана форма элемента `files[]` — `{path, content, mode?}` — и правило: сигнатуру брать из реестра, `get_workflow_details` звать только когда её там нет.
  почему: формы элемента в реестре не было; сессия SYS 2026-09-24 вызвала `get_workflow_details` ради одной строки — ответ около 25 КБ, воркфлоу в нём дважды (рабочая и опубликованная версия).
  кто подтвердил: Андрей, 2026-09-24, «Выполни свои предложения».

Р-7 | В СИЛЕ | 2026-09-24 | Skills с пометкой «ОБЯЗАТЕЛЬНО»
  принято: направление — обязательная часть SKILL.md до 15 КБ, разовые процедуры в references/ по ссылке; первым `second-brain-git`. Вслед за этим сузить триггеры: чтение файла не тянет правила записи и ротации. Исполнение отложено.
  почему: после Р-3 обязательное чтение держат Skills: second-brain-git 71 КБ, n8n-engineering-manual 73 КБ, google-drive-docs 68 КБ, naryad 59 КБ, second-brain-engineer 52 КБ. Сессия, тронувшая repo, читает `second-brain-git` целиком — втрое больше всего старта CORE.
  отвергнуто: 1 — сузить триггеры без разделения: сессия, читающая файл, потеряла бы таблицу выбора транспорта §7.6.
  кто подтвердил: Андрей, 2026-09-24: «это тоже нужно сделать, только потом».
  цена: Skill из нескольких файлов заливается только zip, кнопка Save skill не подходит (google-drive-docs §12).

Р-8 | В СИЛЕ | 2026-09-24 | идеи, проверенные и отвергнутые
  отвергнуто: 3 — (а) удалить серии пустых строк в файлах CORE: замер на 10 файлах — около 380 Б, правка не окупается; (б) сжать формулировки: ужимание, запрещено §5.1; (в) держать копию CORE в документах Claude Project, чтобы не читать Каналом: второй носитель канона, нарушает И-1.
  кто подтвердил: вывод сессии; все три названы владельцу в чате SYS 2026-09-24 как отвергнутые, его ответ «Выполни свои предложения» касался остальных пунктов.


Р-9 | В СИЛЕ | 2026-09-24 | приёмка работы 2026-09-24 независимым Контролёром
  принято: вердикт Контролёра «ПРИНЯТО С ЗАМЕЧАНИЯМИ». Содержание не потеряно: разрез R-06a побайтово совпал со старым, каждый удалённый из STATE git-migration фрагмент найден в журнале, архиве, карте, очереди или NOT_TAKEN.md; арифметика Р-3 и Р-4 сошлась. Исправлено той же сессией: (1) Р-5 и `labs/sys/t-master-prompt-sys.md` утверждали, что сверка byteLength «снята из Skills», — на деле правка выдана владельцу и ждёт заливки; до заливки действует строка 7 `meta/PENDING_RULES.md`; (2) шапка `labs/sys/INDEX.md` называла SYS_INDEX замороженным — теперь «заморозка решена Р-109, исполнение нарядом Н-23.09-05 в репозитории не записано»; (3) STATE git-migration снова называет позицию U-1, возвращённую в открытые в 10:46, и ссылается на Р-4 этого журнала; (4) в `meta/MAP_core-sections.md` сняты неверные размеры Р.6 (R-06b на 2026-09-10 весил 3 613 Б, не 2 374); (5) Манифест больше не утверждает, что любой сдвиг состава обходится без правки мастер-промптов; (6) событие «запись в INBOX» добавлено в таблицу «По событию» — формат INBOX лежит в R-08; (7) STATE этого проекта: 21 146 Б — после правки 4 мастер-промпта, до неё старт 22 727 Б.
  поправки к записям выше (записи не редактируются, 11.1): Р-3 — blob sha по §6.8: `core/R-06a.md` ff6212dd, `core/R-06-marshrut.md` ad1b32c6; Р-4 — закрытое составляло 52 процента файла (9 675 из 18 530 Б), а не две трети; Р-7 — размеры Skills названы по отредактированным копиям, живые на 2026-09-24: second-brain-git 69 569 Б, google-drive-docs 61 967 Б.
  не исправлено, за владельцем: итог приёмки ф3 нигде не записан, а от него зависит старт ф6; маркера T-048 нет за всю историю, а карта пишет диапазон T-044…T-051.
  кто подтвердил: запуск предложен владельцем вопросом «Нужно ли запустить Контролёра, который проверит достигнутый результат?» 2026-09-24; запущен сессией, вердикт принят сессией.
  отвергнуто: 1 — проверять только байты: побайтовая сверка уже была доказана sha до вопроса владельца; ценность Контролёра — в смысле удалённого и в правдивости утверждений, и именно там нашлись три дефекта.

Р-10 | В СИЛЕ | 2026-09-24 | фрагментированное чтение INDEX-файлов и файлов летописи
  принято: вывод исследования (агент-исследователь по заданию владельца «отрицательный результат меня так же устроит»), внедрение ждёт решения владельца. Фрагментировать чтение окупается только для LOG.md: сессия, берущаяся за проект, читает оглавление журнала одним поиском Git Read Lines по шаблону `^(Р|Т)-[0-9]+ \|` и затем только нужные записи по номеру. STATE.md, PLAN.md и INDEX.md лаборатории не фрагментируются.
  почему: замер по клону на коммите b3878a9. Журнал git-migration производит в среднем 21,7 КБ записей в рабочий день (медиана 16,8, пик 66) — больше своего бюджета чтения 16 384 Б, поэтому правило «читать целиком» держалось только на 25 ротациях за 13 дней. Строки-заголовки записей — 4,8 процента журнала, их уже требует форма записи §11, вести оглавление отдельно не нужно. Взятие проекта git-migration: около 34,1 КБ сейчас против около 13,3 КБ плюс открытые записи. В STATE git-migration живое состояние и следующий шаг разбросаны по всем разделам: чтение одного-двух разделов теряет 43 процента живого, экономя около 3,8 КБ. INDEX лаборатории уже устроен как список плюс тела задач; отдельное чтение его строк стоит на 25 Б дороже чтения внутри пакета старта. PLAN почти не растёт; перерос бюджет только PLAN n8n-map (41 798 Б), и то из-за доказательств, место которым по §4.2 в LOG.
  отвергнуто: 12 — (1) оглавление, которое ведут вручную: ручная синхронизация при каждой записи и второй носитель заголовков, а то же оглавление бесплатно выводится поиском; (2) голова файла, первые N строк: строка — абзац переменной длины, журнал растёт вниз и голова отдаёт старое, N — магическое число; (3) разнесение STATE на несколько файлов: пятая роль (4.1), а по 4.5 читателю безразлична раскладка — экономии ноль; (4) файл на каждую запись журнала плюс список: уступает поиску по чтению, дублирует заголовок, переводит запись с Git Append на воркфлоу с утечкой данных исполнения — оставлен в резерве; (5) STATE с чтением только «Следующего шага»: теряет 43 процента живого; (6) журнал только по номеру без оглавления: сессия не знает номеров вне STATE и PLAN — вошёл в принятый вариант второй половиной; (7) фильтр по метке или статусу: старт должен видеть все задачи, а отменённых записей в журнале 1 054 Б из 369 199; (8) фрагментное чтение INDEX лаборатории любым способом: экономия отрицательная; (9) дайджест журнала, который пишут сессии: второй носитель; (10) копия в документах Claude Project: второй носитель, уже отвергнуто Р-8; (11) gdocs-read с диапазоном строк: не экономит контекст, к репозиторию не относится; (12) отменить ротацию совсем: без доработки Git Read Lines оглавление неограниченного журнала растёт само, а файл идёт к пределу GitHub.
  отложено, не отвергнуто: параметры `until` и `tail` для Git Read Lines — чтение раздела до стоп-шаблона одним вызовом и оглавление постоянного размера; нужны правка воркфлоу и зонд до правила (И-3).
  слабое звено: оглавление полезно настолько, насколько поле «вопрос» в строке-заголовке говорит о сути записи; смягчение уже есть — решения, названные в STATE и PLAN, открываются по номеру (на объекте 5 из 6 записей журнала git-migration названы в STATE).
  найдено попутно, кандидат в БЛОКЕРы, решение за владельцем: `projects/git-migration/LOG_ARCHIVE.md` весит 405 129 Б; GitHub отдаёт поле content пустым для файлов больше 1 МБ, а узел github в n8n берёт только content — гипотеза: Git Read по такому файлу вернёт пустой текст без ошибки. При нынешнем темпе предел — примерно через 28 рабочих дней. Зондом не проверено.
  кто подтвердил: вывод исследования; владелец ставил задачу и принимает решение о внедрении.

Р-11 | В СИЛЕ | 2026-09-24 | внедрить чтение журнала LOG.md через оглавление
  принято: вывод Р-10 внедряется. В Skill second-brain-git: таблица §4, строка LOG.md — сессия, берущаяся за проект, читает оглавление журнала и затем только нужные записи по номеру, файл целиком не читается; таблица 7.6 — две строки «оглавление журнала» и «запись журнала по номеру», шаблоны вынесены из таблицы в абзац под ней, потому что знак | внутри ячейки ломает таблицу; §5.2 — ротация наступает, когда выше бюджета чтения выходит оглавление, а не весь файл, и обязательна до размера 1 МБ, после которого GitHub не отдаёт содержимое файла через Contents API; таблица цен 7.6 — у строки «только заголовки записей» названо основание: 0,015× без конверта ответа, около 0,06× с конвертом. До заливки Skill владельцем правило действует строкой 8 `meta/PENDING_RULES.md`.
  почему: Р-10 — журнал растёт быстрее своего бюджета чтения, оглавление бесплатно выводится из формы записи §11 и весит около 5 процентов журнала.
  отвергнуто: 1 — держать прежнее условие ротации «весь файл выше бюджета»: при чтении оглавлением оно заставляло бы ротировать журнал почти ежедневно без выгоды для читателя.
  кто подтвердил: Андрей, в чате SYS 2026-09-24: «Внедряем чтение журнала через оглавление».
  следствие: позиция AF-1 очереди (ротация журнала git-migration) теряет основание после заливки Skill — оглавление журнала git-migration около 1,3 КБ.

Р-12 | В СИЛЕ | 2026-09-24 | нарезка Skills на обязательную часть и справку (рычаг к3)
  принято: second-brain-git и google-drive-docs нарезаны скриптом: существующий текст перенесён по строкам без перепечатки, проверено — каждая строка исходника попала ровно в один файл. Сессия написала только связки: таблицу «Что читать дальше» в начале SKILL.md, указатели на вынесенные пункты и правило Р-11. Номера пунктов сохранены, поэтому ссылки вида «second-brain-git 8.3» и «google-drive-docs §3.2» остаются верными. second-brain-git: 70 991 Б в одном файле → SKILL.md 23 906 Б плюс references/ write 21 064, delivery 8 980, lab 8 092, costs 5 407, rotation 5 293, migration 4 877, core-split 3 080. google-drive-docs: 68 074 Б → SKILL.md 18 005 Б плюс references/ failures 14 435, docs-write 10 823, mcp 10 581, sheets 9 103, docs-read 5 099, infra 3 362. Сессия, которая только читает repo, платит 24 КБ вместо 71; сессия, которая пишет, — около 45 КБ.
  отвергнуто: 3 — (а) сжать формулировки обязательной части до 15 КБ сейчас: это переписывание правил, а не нарезка, с риском потерять смысл; делается отдельным шагом с приёмкой Контролёра; (б) нарезать second-brain-engineer, naryad и n8n-engineering-manual этой же сессией: их полный текст сессия не читала, а решать, что нужно всегда, по одним заголовкам нельзя; (в) сузить триггеры в описании Skill: таблица «Что читать дальше» даёт тот же выигрыш, не рискуя, что Skill вообще не будет прочитан.
  цена: Skill из нескольких файлов заливается только zip-архивом через Customize → Skills, кнопка Save skill не подходит (google-drive-docs §12). Канонический источник google-drive-docs — нативный Google Doc: он не хранит несколько файлов, поэтому источник либо повторяет разбивку разделами, либо перестаёт быть каноническим — решение за владельцем.
  кто подтвердил: Андрей, в чате SYS 2026-09-24: «У тебя хватит контекстного окна нарезать Skill? Если хватит, сделай».
  финиш PLAN.md п.2 (обязательная часть до 15 КБ) не достигнут: 23,9 и 18,0 КБ.

Р-13 | В СИЛЕ | 2026-09-24 | принципиально иные способы уменьшить стоимость чтения, кроме нарезки и таблиц CORE
  принято: перечень кандидатов записан в PLAN.md рычагами к8–к13, решение по каждому — за владельцем. к8 — чтение через посредника: тяжёлое чтение делает субагент и возвращает только ответ на вопрос, а не текст; к9 — узкие читатели на стороне n8n: воркфлоу, отдающий только сигнатуру входа другого воркфлоу, и чтение раздела markdown по заголовку; к10 — тонкий мастер-промпт: роль, обязательный формат анализа и режим старшего инженера уходят в Skill, в промпте остаются старт и ссылки; к11 — запись фрагментами: тело каждого вызова записи остаётся в контексте сессии до её конца; к12 — ответ короче прочитанного: развёрнутое уходит в файл, в чат — итог; к13 — правила в виде таблиц решений вместо прозы, путь к 15 КБ обязательной части Skill.
  почему: сегодняшний замер сессии SYS — самое дорогое в контексте не CORE (21 КБ), а Skills (70 КБ на один только second-brain-git до нарезки), ответы get_workflow_details (около 25 КБ каждый, три вызова), тела вызовов записи (Т-1) и длинные ответы в чат. Контролёр и исследователь как субагенты прочитали по 200 тысяч токенов каждый, а в контекст сессии вернули около 10 КБ отчёта на двоих — это и есть к8 на деле.
  отвергнуто: 4 — (а) чтение разницы с прошлой сессией: у новой сессии нет копии прошлого текста, к которой применять разницу, а память Claude как носитель запрещена принципом 3 мастер-промпта; (б) CORE прямо в мастер-промпте или в документах Claude Project: второй носитель канона (И-1); (в) поиск по смыслу (векторный индекс) по правилам: пропущенное правило не видно, а для контракта записи пропуск недопустим — допустим только для поиска по архиву журнала; (г) сокращения и аббревиатуры в тексте правил: владелец их запрещает, а экономия съедается ошибками понимания.
  кто подтвердил: вывод сессии по вопросу владельца «Есть ли ещё принципиально иные методы уменьшить стоимость чтения» 2026-09-24; внедрение — по слову владельца.

Т-1 | 2026-09-24 | запись в репозиторий целыми файлами через Git Write и Git Commit Files в сессии без клона с push
  почему не вышло: тело вызова остаётся в контексте сессии до её конца, а харнесс сериализует кириллицу во входе инструмента экранированными последовательностями \uXXXX — замер на projects/kontext-reduktion/LOG.md: 17 061 символ текста дают 69 498 символов экранированного JSON, в 4,05 раза больше; попытка писать сырой кириллицей на это не повлияла. Оценка по тому же множителю: один вызов Git Commit Files этой сессии с пятью файлами летописи (около 21 тысячи символов текста) занял в контексте около 85 тысяч символов.
  условие возврата: среда, где `git push --dry-run` проходит (запись идёт из файла, тело не проходит через вызов инструмента), либо воркфлоу записи, берущий тело из файла на Drive по образцу gdocs-apply-from-file.
  стоимость попытки: заметная доля контекста сессии; вывод — рычаг к11 PLAN.md.

Р-14 | ОТМЕНЕНО Р-15 | 2026-09-24 | откуда множитель 4,05 в Т-1 и сколько стоит кириллица в теле вызова инструмента
  принято: вывод замера — множитель Т-1 настоящий, но его даёт набор кириллицы в теле вызова последовательностями \uXXXX, а не харнесс. Кириллица, набранная в теле вызова как есть, стоит примерно втрое меньше. К такому набору подталкивает пункт 8.2 second-brain-git («текст передаётся JSON-экранированным»): python json.dumps по умолчанию (ensure_ascii=True) заменяет каждую букву кириллицы шестью символами \uXXXX. Правка пункта 8.2 — позиция AG-3 очереди.
  почему: замер в облачной сессии Claude Code 2026-09-24 по полю usage журнала сессии, в токенах, а не в символах. Один абзац (Р-5 этого журнала, 576 символов, 909 Б) записан инструментом Write трижды, каждый вызов отдельным ходом. По-английски — 339 токенов выхода; русский как есть — не больше 539 (в число вошёл короткий блок рассуждения модели перед вызовом); русский с \uXXXX, набранными моделью, — 1 560. Прирост контекста на следующем ходу превышал выход вызова на 104–106 токенов во всех трёх случаях: тело хранится в контексте в том виде, в каком модель его набрала, харнесс его не раздувает. Файл, записанный из \uXXXX, вышел на диске побайтово равным русскому (576 символов, 909 Б): экранирование снимается при разборе JSON и оплачивается только моделью, которая его набирает. Т-1 утверждал, что попытка писать сырой кириллицей на множитель не повлияла, — этот замер утверждение опровергает.
  вывод сессии, проверяемый арифметикой: (1) 1 560 / 539 = 2,9 — нижняя граница множителя по полному вызову, без служебной части вызова (путь к файлу) он выше трёх; (2) число Т-1 совпадает с json.dumps: 17 061 + 5 × 10 487 = 69 498, то есть ровно замена каждой кириллической буквы шестью символами при доле кириллицы 61,5 процента; доля кириллицы в этом журнале на 2026-09-24 — 61,7 процента (12 343 из 20 003 символов), json.dumps с ensure_ascii=True даёт на нём 4,14, с ensure_ascii=False — 1,0.
  попутно: русский дороже английского на этом абзаце примерно на 30 процентов (не больше 539 против 339 токенов; в абзаце много латинских имён).
  отвергнуто: 2, сессией — (а) запись фрагментами (рычаг к11 PLAN.md) как лекарство от Т-1: уменьшает объём тела, но не способ набора, и фрагмент в \uXXXX стоит так же втрое дороже; (б) перевести на английский правила, которые читает агент: около 30 процентов экономии ценой второго носителя (И-1).
  допущение: замер снят в облачной сессии Claude Code, а не в Claude Project; тело вызова в текст для модели переводит API, поэтому перенос вывода на Claude Project вероятен, но зондом там не проверен.
  кто подтвердил: Андрей, 2026-09-24, «Пиши» — на предложение записать замер и поправку к Т-1.
  обратимость: дёшево — новая запись отменяет эту.

Р-15 | В СИЛЕ | 2026-09-24 | отмена главного вывода Р-14: тело вызова раздувает харнесс, но только у параметра-объекта
  принято: Р-14 отменяется. Её вывод «множитель даёт набор модели, а не харнесс» для Канала неверен. Тело, переданное параметром-объектом (у инструмента MCP n8n execute_workflow это inputs), хранится в контексте последовательностями \uXXXX, как бы модель его ни набрала; параметр-строка верхнего уровня (content у Write) хранится так, как набран. Т-1 в главном верен, включая утверждение, что запись сырой кириллицей не помогает: модель на это не влияет. Позиция AG-3 очереди, названная в Р-14, не заведена: предложенная там правка пункта 8.2 Канал не удешевляет.
  почему: замер той же сессии на записи самой Р-14. Вызов Git Append через execute_workflow с телом 4 610 Б: выход модели 1 429 токенов, прирост контекста на следующем ходу 8 175 токенов при ответе инструмента в одну строку ({"executionId":"68331","status":"started"}). У трёх вызовов Write из Р-14 прирост превышал выход на 104–106 токенов. В журнале сессии тело этого вызова хранится обычной кириллицей, а в контексте модели тот же вызов виден последовательностями \uXXXX: экранирование добавляется при переводе вызова в текст для модели.
  вывод сессии, проверяемый арифметикой: 8 175 / 1 429 = 5,7 — множитель в токенах для тела с долей кириллицы около 60 процентов; Т-1 называл 4,05 в символах.
  допущение: модель видит свои прошлые вызовы экранированными и, вероятно, поэтому сама начинает набирать \uXXXX (Т-66 и Т-74 журнала git-migration, Т-75 там же) — не проверено.
  кандидат, решение за владельцем: тело кладётся на Drive инструментом, у которого содержимое — параметр-строка верхнего уровня, а воркфлоу записи получает только ID файла, по образцу gdocs-apply-from-file (второе условие возврата Т-1); сначала — зонд, что у этого инструмента тело в контексте не раздувается.
  кто подтвердил: Андрей, 2026-09-24, «Пиши»; отмена Р-14 — вывод сессии по замеру, снятому при исполнении этого слова.
  обратимость: дёшево — новая запись отменяет эту.

Р-16 | В СИЛЕ | 2026-09-24 | тело записи мимо параметра-объекта: зонд и воркфлоу Git Append File
  принято: (1) зонд: create_file Google Drive передаёт тело параметром-строкой textContent, и в контексте оно хранится так, как набрано — тело 3 439 Б (текст Р-15): выход 1 260 токенов вместе с коротким рассуждением, прирост контекста 1 545 при ответе инструмента около 250 токенов; тот же текст через Git Append — 6 165 токенов выхода. (2) Чтение не раздувается: Git Read файла meta/PENDING_RULES.md (2 917 Б) с сужением до 05_Respond — прирост контекста 1 670 токенов вместе с конвертом ответа, кириллица в ответе приходит как есть. (3) Создан воркфлоу Git Append File (Nzgy6Q1XwXQVxt1F, не опубликован): вход {path, fileId}; текст берётся из файла Drive запросом alt=media под сервисным аккаунтом; дальше логика Git Append без изменений — GET, склейка через перевод строки, PUT с sha, повторный GET. Git Append не тронут. (4) Папка для таких файлов — _git_staging (10JCxPrSucgOfMMRLV9xe87rvq-PtLowm) внутри _SYSTEM, выдана сервисному аккаунту n8n-liamin на чтение.
  почему: Р-15 — тело в параметре-объекте стоит в контексте 5,7 раза дороже набранного; через файл Drive в вызове n8n остаётся только ID.
  к Р-15: допущение подтвердилось той же сессией — второй вызов Git Append (запись Р-15) модель набрала последовательностями \uXXXX сама: 6 165 токенов выхода при приросте контекста 6 210.
  отвергнуто: 2, сессией — (а) добавить вход fileId в сам Git Append: пять затронутых узлов при пороге три (second-brain-engineer, ЗАПРЕТ 2) и риск снять привязку credential с рабочего канала записи (R1b n8n-engineering-manual); (б) узел GitHub вместо HTTP для записи: его правка файла сама берёт sha перед записью, и чужая дозапись между чтением и записью терялась бы молча.
  блокер, за владельцем: привязать credential руками в редакторе (R1a, через MCP нельзя): 01_Webhook — git-channel-webhook-auth (автопривязка поставила WB API Token_Customer Support); 02B_Get_Text — Google сервисный аккаунт n8n-liamin; 03_Get_Before, 05_Put_File, 06_Get_After — github-canon-token. Прогон exec 68345 упал на 02B_Get_Text: «Credentials not found». После привязки — зонд, публикация, строка в registry/tools.md (И-3).
  попутно: у gdocs-overwrite v2 (M5dqSv2yiklnDXoP) вебхук открыт — triggerInfo «No credentials required» (R12).
  кто подтвердил: Андрей, 2026-09-24, «Делай, а не пингуй».
  обратимость: дёшево — воркфлоу архивируется, папка удаляется.

Р-17 | В СИЛЕ | 2026-09-24 | Git Append File принят в Канал
  принято: воркфлоу Git Append File (Nzgy6Q1XwXQVxt1F) опубликован и становится путём дозаписи в append-only файл для текста, набранного сессией. Порядок: текст — файлом в папку _git_staging через create_file Google Drive (textContent, contentMimeType text/plain, disableConversionToGoogleType true, parentId 10JCxPrSucgOfMMRLV9xe87rvq-PtLowm); затем execute_workflow с телом {path, fileId}; проверка по 8.1 second-brain-git, как у Git Append; после проверки файл-источник — в корзину. Git Append остаётся запасным путём и для дозаписей в несколько строк.
  почему: зонд exec 68351 после привязки credential владельцем: в test/probe-N-01.09-01.txt дописан файл 135 Б с кириллицей, кавычками-ёлочками, тире и эмодзи вне BMP; прирост файла 136 Б = 135 + 1, хвост совпал байт в байт (коммит 361bb92). Публикация: versionId равен activeVersionId, triggerInfo требует заголовок Authorization. Эта запись легла тем же путём; её цена в контексте — в строке Git Append File реестра registry/tools.md.
  попутно: сохранение в редакторе убрало из узла 02B_Get_Text параметр outputPropertyName — его значение data совпадает с умолчанием; класс R17 n8n-engineering-manual, на работу не повлияло.
  кто подтвердил: Андрей, 2026-09-24 — привязал credential («credentials sind eingestellt»); слово «Делай, а не пингуй».
  обратимость: дёшево — снять воркфлоу с публикации.

Р-18 | В СИЛЕ | 2026-09-24 | вторая нарезка n8n-engineering-manual и naryad по событию; перевод Skills на английский
  принято: обязательная часть двух Skills разрезана по событию скриптом по номерам строк и якорным фразам, без перепечатки; каждая непустая строка исходника попала ровно в один файл (проверено сравнением мультимножеств строк), номера пунктов сохранены. n8n-engineering-manual: SKILL.md 50 904 Б → 17 544 Б; новые references/engine.md 10 718 Б (§4, читать перед проектированием или правкой логики воркфлоу) и references/roots.md 24 666 Б (§5 и §7, читать перед диагностикой). naryad: SKILL.md 45 204 Б → 24 094 Б; новые references/issuer.md 14 362 Б (3.2, 3.5, 3.6, 3.9, контракт Руководителя), foreman.md 2 631 Б (5), executor.md 6 563 Б (6); строка 131, где контракты Прораба и Исполнителя были склеены в одну, разрезана по фразам «5. Контракт Прораба» и «6. Контракт Исполнителя». Сессия написала только связки: строки таблиц «Что читать дальше», заголовки новых файлов, поправки абзаца «Как устроен Skill» и описания naryad. Чтение по событию: n8n — вызов или публикация 17,5 КБ, запись воркфлоу около 35,8 КБ (SKILL.md, write.md, engine.md), диагностика около 42 КБ; naryad — Исполнитель 30,7 КБ, Руководитель 38,5 КБ; до нарезки — 50,9 и 45,2 КБ при любом событии. Заливка — позиции AG-6 и AG-7 очереди.
  почему: замер сессии — чтение SKILL.md n8n-engineering-manual стоило около 14 500 токенов, а разделы §4, §5 и §7 нужны только при правке логики воркфлоу или диагностике.
  отвергнуто: 1, сессией по замеру; владелец выбрал нарезку — перевод Skills на английский: delivery.md second-brain-git, прочитанный одним способом, стоит 2 303 токена по-русски и 2 039 по-английски, экономия около 11 процентов ценой перевода около 400 КБ правил, риска сместить их смысл и второго носителя, если хранить обе версии.
  не достигнуто: финиш PLAN.md п.2 (15 КБ) — n8n 17,5 КБ, naryad 24,1 КБ; naryad держит в обязательной части 3.1 и 3.1а, потому что по ним Исполнитель проверяет ОСНОВАНИЕ ГДЕ и МОДЕЛЬ при приёме (§6, пункты а и в).
  попутно, за владельцем: в naryad 3.1, вопрос 1, сказано «Канал n8n из Claude Code недоступен (Н-10.09-05)» — облачная сессия Claude Code 2026-09-24 весь день вызывала Канал через MCP n8n; строка версии naryad «1.11 … updated 2026-09-17» не отражает ни нарезки Р-13, ни этой; раздел «Контракт Руководителя» потерял номер 4, несколько абзацев склеены в одну строку — следы переноса из Doc.
  кто подтвердил: Андрей, 2026-09-24: «mach пункт 2 для n8n-engineering-manual и naryad».
  обратимость: дёшево — текст правил не менялся, только разложен; прежний файл собирается склейкой частей.

Р-19 | В СИЛЕ | 2026-09-24 | нарезка apps-script-sheets по событию
  принято: тем же способом, что Р-18 (скрипт по номерам строк, каждая непустая строка исходника ровно в одном файле, номера параграфов сохранены), SKILL.md 49 067 Б → 25 225 Б. В обязательной части остались §0, §1, §2, §4, §6, §7, §8 — их требует любой скрипт над книгой, а таблица ошибок §6 служит указателем на все параграфы. В references/ ушли: book-traps.md 5 497 Б (§3, читать перед правкой или сборкой формул витрины), design.md 7 426 Б (§5, перед оформлением, графиками, текстом тревоги), signal.md 14 555 Б (§9, перед постройкой сигнала и назначением порога). Два разделителя «---» между вынесенными параграфами не перенесены. Сессия написала только связки: абзац «Как устроен Skill», таблицу «Что читать дальше», заголовки новых файлов и предложение в описании (длина 442 символа). Чтение по событию: оформление — около 32,7 КБ, формулы витрины — около 30,7 КБ, сигнал — около 39,8 КБ; до нарезки — 49,1 КБ при любом событии. Заливка — позиция AG-8 очереди.
  почему: Skill — самый тяжёлый без нарезки (49 КБ), а §3, §5 и §9 нужны не каждому скрипту.
  попутно, за владельцем: (1) §5 держит пороги в колонках листа («Колонка «порог ₽» и колонка «порог %»»), а §9.5 велит хранить пороги сигнала в коде и оставляет в ячейках только пороги тревог — не сказано, к какому виду относятся колонки §5; (2) §8 называет англоязычную копию DASHBOARD_BLUEPRINT_EN, которая «живёт файлом», без адреса файла — найти её по Skill нельзя.
  кто подтвердил: Андрей, 2026-09-24: «loss» на предложение взяться за apps-script-sheets.
  обратимость: дёшево — текст правил не менялся, только разложен.

Р-20 | В СИЛЕ | 2026-09-24 | замер старта SYS в токенах; нарезка second-brain-engineer; правки naryad и second-brain-git 9.4
  принято: (1) замер старта SYS в облачной сессии Claude Code: пакет старта (meta/PENDING_RULES.md, meta/header.md, core/MANIFEST.md, labs/sys/INDEX.md и 4 файла таблицы «Список», 26 438 Б) стоит 9 598 токенов через Git Read Batch с сужением до 05_Respond и 8 083 токена чтением из клона, то есть Канал дороже клона примерно на 19 процентов; SKILL.md second-brain-engineer и second-brain-git, которые читаются на том же старте, — около 43 КБ после заливки AG-9, около 11 600 токенов при 3,7 Б на токен (соотношение по замеру чтения SKILL.md second-brain-git в этой сессии: 23 906 Б — 6 409 токенов), больше половины старта. (2) second-brain-engineer нарезан тем же способом, что Р-18: SKILL.md 25 262 Б → 19 110 Б; новые references/save.md 5 062 Б (Р7 !save и Р12 гигиена Зоны А вместе с абзацем правки 2026-09-24 вторая: в нём единственный носитель правила, как !save читает CORE, и он переехал к Р7, а не в историю) и references/history.md 2 723 Б (абзацы правок 2026-09-16 и 2026-09-24 первая: их правила уже стоят в Р8, Р11, Р13, Р14); каждая непустая строка исходника ровно в одном файле, кроме двух разделителей «---»; попутно исправлена позиция AG-2 очереди — в Р1 «актуальность даёт git log (meta/header.md)» заменено на «актуальность даёт git log и blob sha». (3) naryad: утверждение 3.1 «Канал n8n из Claude Code недоступен (Н-10.09-05)» заменено правилом проверять наличие Канала в самой сессии по списку её инструментов, как push; версия 1.12; разделу «Контракт Руководителя» в references/issuer.md возвращён номер 4. Новое правило о Канале — решение владельца: оно вступает в силу только заливкой zip. (4) second-brain-git references/delivery.md 9.4(а) называет Git Append File основным путём дозаписи, Git Append — запасным. (5) PLAN.md: рычаги к3 и к11 получили состояние, добавлены к14 (срабатывание Skill) и к15 (чтение из клона). Заливка — позиции AG-9, AG-10, AG-11 очереди.
  почему: слово владельца «делай все что можешь без меня»; замер показал, что после нарезки CORE и Skills самый дорогой остаток старта SYS — два SKILL.md, читаемые каждой сессией.
  отвергнуто: 1, сессией — нарезать second-brain-git ещё раз: его обязательная часть держит инварианты, таблицы 4 и 7.6 и правила чтения, которые нужны любой сессии с репозиторием; кандидатов по событию в ней не осталось без переписывания правил.
  допущение: 11 600 токенов для двух SKILL.md посчитаны по соотношению байт к токенам, а не замером чтения обоих файлов; соотношение снято на одном из них в этой же сессии.
  кто подтвердил: Андрей, 2026-09-24: «делай все что можешь без меня».
  обратимость: дёшево — текст правил, кроме правила о Канале в naryad, не менялся, только разложен.

Р-21 | В СИЛЕ | 2026-09-24 | самый дорогой файл канона вне старта: очередь на обслуживание; рычаг к16
  принято: (1) Замер состава `registry/maintenance-queue.md` по клону 2026-09-24: файл 161 496 Б, 143 строки-позиции. Из них 96 закрытых (ВНЕСЕНО, ЗАКРЫТО, ДУБЛЬ, СНЯТ) — 70 835 Б, и 47 открытых — 36 197 Б, в среднем 770 Б на открытую позицию при собственном правиле файла «позиция несёт формулировку в одну-две строки». Не-табличного текста 47 167 Б — это датированные разделы-разборы; крупнейшие по прозе: «Исправление коллизии номеров» 5 055 Б, «Возврат позиции U-1» 4 613 Б, «second-brain-engineer Р2/Р3 vs Р11» 4 603 Б, «Поправка к N-1 и итог сессии Cowork» 3 876 Б, «Решение владельца: лаборатория» 3 332 Б, «Закрытие P-1» 3 295 Б. Остальные 5 098 Б — заголовки и разделители таблиц. (2) В PLAN.md заведён рычаг к16 с двумя мерами; решение по каждой за владельцем: (а) проза разборов уходит в LOG.md того проекта, который её породил (в основном git-migration), в очереди остаётся указатель — это перенос материала к читателю по 5.1 second-brain-git, а не ужимание; (б) 96 закрытых позиций уходят в `registry/maintenance-queue-archive.md` тем же порядком, каким записи LOG.md ушли в LOG_ARCHIVE.md. После обеих мер очередь — около 42 КБ вместо 161 496 Б. (3) Основание, записанное в шапке очереди против удаления строк — «строка не удаляется, DELETE каналом записи не покрыт (§6a second-brain-git)» — больше не действует: Git Commit Files назван единственным способом удалить путь (8.3 references/write.md), а пересборка файла из уже существующего текста идёт маршрутом 9.4(б). (4) Исправлено в этой сессии: `meta/PROTOCOL_harvest.md` шаг 3 предписывал искать позиции очереди через Git Grep — единственный инструмент Канала, у которого contextData несёт base64 всего файла и nodeNames его не режет (Р-2 `meta/CONTRACT_file-limits.md`, exec 63960, множитель 1,50× снят на файле 17 930 Б). На очереди 161 496 Б такой поиск дороже, чем прочитать очередь целиком. Заменено на Git Read Lines с pattern, в среде с клоном — grep; вывод сессии: Git Grep в составе Канала (§0 second-brain-git) не назван вовсе, то есть сессия, идущая по Skill, о нём и не знает. (5) Исправлено там же: `meta/CONTRACT_file-limits.md` держал снимок «maintenance-queue.md 90 416 Б» — обновлён до 161 496 Б с указателем на эту запись; прирост 79 процентов.
  почему: замер старта SYS (Р-20) закрыл вопрос о цене старта, но не о самом дорогом единичном чтении системы. Очередь — единственный файл канона, у которого нет ни бюджета чтения в таблице 4 second-brain-git, ни правила ротации, тогда как LOG.md получил и то и другое; и ровно у неё протокол чтения предписывал самый дорогой транспорт из существующих.
  отвергнуто: 1, сессией — ставить очереди потолок записи в байтах: потолок записи отменён правилом происхождения текста (4.5 references/write.md), а отказ записать позицию из-за размера файла прямо запрещён (5.5). Лечится не потолком, а вывозом закрытого и прозы.
  кто подтвердил: замер и правки (4), (5) — вывод этой сессии по слову Андрея «kontext-reduktion, продолжаем» 2026-09-24. Решение по мерам (а) и (б) рычага к16 — за владельцем: обе удаляют строки из файла repo и требуют явного «да» (Р13 second-brain-engineer).
  обратимость: правки (4) и (5) — дёшево, по одному Git Replace каждая, прежний текст в истории коммитов. Меры (а) и (б) не выполнялись.

Т-2 | 2026-09-24 | push в origin/main из облачной сессии Claude Code
  почему не вышло: `git push --dry-run` отбит прокси сессии кодом 403 с текстом «lyamin-dot/second-brain-canon is not in this session's authorized repository set, so the proxy will not inject a credential for it. To fix, add the repository to the session's sources». Отказ не в правах GitHub и не в клоне: анонимный клон того же репозитория в этой же сессии работает и им сделаны все проверки записи. Отказ — в списке источников самой сессии. Это уточняет стоявший с 2026-09-05 вывод «push из облачной песочницы закрыт прокси, чтение разрешено»: закрыт не прокси как таковой, а сессия, у которой репозитория нет в источниках.
  условие возврата: владелец добавляет `lyamin-dot/second-brain-canon` в источники сессии Claude Code и повторяет ту же пробу. Если push открывается — маршрут 9.4(б) исполним из облака, и позиции AD-1 и AF-1 очереди, стоящие со статусом «ждёт среды с открытым push», перестают ждать среды.
  стоимость попытки: один `git push --dry-run`, ни одной записи.

Р-22 | В СИЛЕ | 2026-09-24 | финиш PLAN.md п.2 заменён критерием; конструкция рычага к16 и готовый скрипт маршрута 9.4(б)
  принято: (1) Финиш PLAN.md п.2 «обязательная часть каждого Skill — не больше 15 КБ» заменён критерием «содержит только то, что нужно любой сессии в момент чтения»; измеримым финишем назначена стоимость старта в токенах у названного читателя — старт сессии SYS, замер 2026-09-24: около 19 700 токенов. (2) Пункт (2а) Р-21 — «проза разборов уходит в LOG.md проекта-источника» — заменён: прозу и закрытые позиции принимает один файл `registry/maintenance-queue-archive.md` рядом с очередью; остальные пункты Р-21 в силе. (3) Разрез собран и принят в этой сессии на клоне: очередь 161 496 → 44 307 Б (27 процентов от прежнего), архив 124 153 Б; 47 открытых позиций остаются в очереди, 96 закрытых уезжают. Шесть условий приёмки пройдены: ни одна непустая строка источника не потеряна; не-строительные строки не продублированы (заголовки разделов и строительные строки таблиц дублируются намеренно — они не содержание, а каркас); нового текста только 5 строк шапки архива; сумма позиций 47 плюс 96 равна 143 из источника; в очереди не осталось закрытых позиций; в архиве нет открытых. (4) Доставка не выполнена и Каналом невыполнима по двум независимым причинам: правило 4.5 запрещает набирать существующий текст моделью, и в Канале с 2026-09-16 стоит замок — Git Write и Git Commit Files append-only файл не перезаписывают, а очередь в их числе (8.3). Единственный маршрут — 9.4(б). Скрипт маршрута написан и прогнан на клоне без записи: сам клонирует репозиторий, режет, проходит шесть условий приёмки и при любом непройденном останавливается до записи, затем сверяет blob sha записанного с намеренным и толкает push перемоткой, проверив, что origin/main не уехал с момента клона. Ждёт среды с открытым push — условие возврата Т-2.
  почему: 15 КБ достижимы только переписыванием правил, что запрещено инвариантом 1 этого плана, и у числа нет названного читателя (И-5 second-brain-git) — число без читателя и даты снятия не норма, а слух. По пункту (2): 22 из 40 разделов прозы называют запись-источник в журнале проекта, и соблазн снять их как дубль велик, но доказательство дубля требует чтения 22 записей и суждения о содержании — ровно тот класс работы, который на переносе Зоны А SYS_INDEX стоил четырёх кругов Контролёра. Механический перенос принимается мультимножеством строк; суждение о содержании не принимается ничем.
  отвергнуто: 1, сессией — снять прозу с названным источником как дубль (30 123 Б из 45 768 Б прозы в разделах крупнее 200 Б). Причина отказа выше. Мера остаётся кандидатом на отдельный проход, и после разреза он дешевле: искать дубли в файле 44 КБ дешевле, чем в 161 КБ.
  кто подтвердил: Андрей, 2026-09-24: «давай делать то, что ты предложил» — в ответ на предложение заменить число критерием и вывезти из очереди закрытое и прозу.
  обратимость: разрез не доставлен, откат не нужен. После доставки откат — возврат версии из истории коммитов тем же маршрутом 9.4(б).
  допущение: 19 700 токенов — сумма замера пакета старта из клона (8 083, Р-20) и расчёта двух SKILL.md по соотношению байт к токенам (около 11 600, допущение Р-20), а не замера чтения обоих файлов.

Р-23 | В СИЛЕ | 2026-09-24 | рычаг к16 доставлен: очередь 163 075 → 46 943 Б, заведён архив очереди
  принято: Меры (а) и (б) рычага к16 доставлены в main сессией Claude Code, созданной на репозитории, маршрутом 9.4(б) скриптом `k16-rotate-queue.py`; коммит 46f8441. Независимая приёмка выполнена этой сессией по объекту, чтением с origin/main после fetch, а не по отчёту исполнителя: `registry/maintenance-queue.md` 163 075 → 46 943 Б (28 процентов от прежнего), заведён `registry/maintenance-queue-archive.md` 124 153 Б; в очереди 48 позиций и среди них 0 закрытых, в архиве 96 позиций и среди них 0 открытых, сумма 48 плюс 96 равна 144 — числу позиций очереди до вывоза; из 358 непустых строк прежней очереди ни в одном из двух файлов не найдена ровно одна — заменённое правило ведения «строка не удаляется, DELETE каналом записи не покрыт», и замена намеренная; позиция AG-12, дописанная в очередь за полчаса до вывоза, в ней сохранилась. Шапка очереди получила указатель на архив и отмену прежнего основания против удаления строк. Вывод сессии, проверяемый внутри записи: при соотношении 3,7 Б на токен (замер Р-20) чтение очереди целиком стоило около 44 000 токенов, а стоит около 12 700.
  почему: очередь была самым дорогим единичным чтением системы — в шесть раз больше всего пакета старта SYS (26 438 Б), при том что 44 процента её объёма составляли закрытые позиции, а 29 процентов — проза разборов, то есть материал, который не меняет действия ни одной сессии (рабочий вопрос 5.1 second-brain-git).
  кто подтвердил: Андрей, 2026-09-24: выполнил оба шага пути А и прислал вывод обеих команд; решение о мерах — Р-21 и Р-22.
  обратимость: дёшево — возврат версии очереди из истории коммитов и удаление архива, тем же маршрутом 9.4(б); текст ни одной строки не переписан, поэтому откат механический.
  попутно: условие возврата Т-2 наступило, и в форме, отличной от записанной. Push открывается не добавлением репозитория в источники идущей сессии — такого переключателя нет, набор источников задаётся при создании сессии, — а созданием сессии Claude Code НА репозитории `lyamin-dot/second-brain-canon`. Это же объясняет, почему ротации LOG.md 2026-09-16 прошли без затруднений. Следствие: позиции AD-1 и AF-1 очереди, стоящие со статусом «ждёт среды с открытым push», исполнимы в такой сессии и среды больше не ждут.

Р-24 | В СИЛЕ | 2026-09-24 | разбор STATE.md по 5.1: что в нём оказалось не по роли
  принято: STATE.md проекта разобран по 5.1 second-brain-git. Снято пять видов материала. (1) Снимки чисел — замер старта SYS, размеры обязательных частей шести Skills, состав очереди до вывоза: они устаревают при первой правке любого файла и живут в Р-18…Р-23; в STATE остался указатель на записи. (2) Закрытое с датой — четыре пункта «ВЫПОЛНЕНО 2026-09-24» раздела «Ждёт владельца» и строка о находках naryad: вывезены блоком хроники в LOG.md той же записью. (3) Дубли PLAN.md — «рычаги к8–к13 кандидаты» и «финиш ждёт подтверждения» уже стоят в таблице рычагов и разделе «Финиш» PLAN.md. (4) Дубль внутри самого файла — обязательство вычистить строки 7 и 8 `meta/PENDING_RULES.md` стояло в трёх разных разделах из четырёх; осталось в одном. (5) Две секции, делавшие одну работу, — «Следующий шаг» и «Что стоит первым в следующей сессии» — слиты в одну. Оставлено то, что меняет действие следующей сессии: обязательства владельца списком, два килобайта, снимаемых со старта без новых решений, и следующий шаг.
  почему: файл дорос до 7 588 Б при бюджете чтения 8 192 Б (таблица 4), а рабочий вопрос 5.1 — «изменит ли строка действие следующей сессии» — отсёк больше половины: четыре пункта раздела «Ждёт владельца» из семи означали «уже сделано», и ни один из них ничьё действие не менял.
  отвергнуто: 1, сессией — вывезти закрытое одним указателем на журнал, без блока хроники, как дубль. Проверено чтением записей: факт заливки Skill владельцем и побайтового совпадения живых копий с выданными в Р-17…Р-20 не стоит нигде, он существовал только в STATE.md. Указатель на несуществующий адрес потерял бы факт.
  кто подтвердил: Андрей, 2026-09-24: «Делай по плану: сначала первый пункт» — первым пунктом плана был разбор этого файла. Тем же сообщением снят с очереди дел рычаг к6: «Проекты пока не переносим — нужно разобраться и настроить систему».
  обратимость: дёшево — прежний STATE.md целиком лежит в истории коммитов, блок хроники его возврату не мешает.
  попутно: 5.5(2) велит вывозить закрытое с датой блоком хроники в LOG.md, а 11.3 запрещает ретроспективную запись — блок описывает события чужих сессий. Формы для этого случая правила не дают; сессия пометила блок как перенос, а не как свидетельство. Позиция AG-14 очереди.

ХРОНИКА | перенесено из STATE.md разбором по 5.1, 2026-09-24 (Р-24)
  Это не свидетельство сессии, а перенос закрытых строк из раздела «Ждёт владельца» STATE.md. Решения по ним — Р-11, Р-12, Р-17…Р-23; здесь только факты закрытия, которых в тех записях нет.
  2026-09-24: три Skill залиты владельцем, живые копии побайтово совпали с выданными; `core/R-06c.md` удалён вместе со строкой Манифеста и правкой карты разделов (позиция Q-4 закрыта), позиция AF-1 закрыта (Р-11).
  2026-09-24: credential привязаны владельцем, Git Append File опубликован и внесён в `registry/tools.md` (Р-17).
  2026-09-24: позиции AG-3…AG-7 внесены владельцем — путь Git Append File в second-brain-git, исключения в описаниях google-drive-docs и n8n-engineering-manual, вторая нарезка n8n-engineering-manual и naryad (Р-17, Р-18); живые копии побайтово равны выданным.
  2026-09-24: позиции AG-9, AG-2, AG-10, AG-11 внесены владельцем, живые копии побайтово равны выданным; заливкой naryad владелец согласился с правилом проверять наличие Канала по списку инструментов самой сессии (Р-20).
  2026-09-24: три находки в naryad (Р-18) исправлены в zip позиции AG-10.

Р-25 | В СИЛЕ | 2026-09-24 | замер рычага к8: посредник выигрывает только на ответе инструмента, и платит за это расходом
  принято: Замер «до» по инварианту 4 плана снят тремя пробами разной формы в этой сессии; каждому посреднику вменялось назвать, сколько байт он прочитал сам, и каждый ответ проверен мной по объекту. (1) Проба А, справка Skill `references/roots.md` 24 666 Б, вопрос о корневой причине и адресе фикса: посредник прочитал grep-ом около 5 800 Б, вернул в контекст около 530 Б, ответ верен, включая адрес «§2.3, T4 гейт 6» (строка 18 файла); моя проверка стоила около 3 100 Б. (2) Проба Б, `registry/maintenance-queue-archive.md` 124 153 Б, вопрос о возврате позиции U-1: посредник прочитал около 2 700 Б, то есть 2 процента файла, вернул около 1 500 Б, ответ верен; моя проверка стоила 614 Б. (3) Проба В, ответ инструмента `get_workflow_details` о воркфлоу M5dqSv2yiklnDXoP: посредник получил около 6 800 Б, вернул около 1 100 Б, ответ верен и заодно подтверждает находку Р-16 об отсутствии аутентификации на вебхуке; харнесс сообщил расход посредника — 85 042 токена, три вызова инструментов, 12 секунд.
  почему: Р-13 занёс к8 в кандидаты на основании наблюдения, что Контролёр и исследователь прочитали по 200 тысяч токенов каждый и вернули около 10 КБ на двоих. Наблюдение верно, но вывод из него был шире факта: он приписал посреднику всю разницу между размером объекта и размером ответа.
  вывод сессии, проверяемый арифметикой внутри записи: на ФАЙЛАХ посредник почти ничего не добавляет к уже действующему правилу 7.6. Оба посредника читали не файл целиком, а срез по pattern — 2 процента в пробе Б и 24 процента в пробе А, — а это и есть то, что 7.6 предписывает делать самой сессии. Выигрыш посредника поверх правильно применённого 7.6: в пробе Б 1 500 Б ответа против 2 700 Б среза, и он исчезает целиком, стоит мне ответ проверить — 1 500 плюс 614 равно 2 114 против 2 700, то есть 22 процента, которые не стоят ни задержки, ни расхода. В пробе А выигрыш больше — 530 против 5 800, — но и там проверка съедает больше половины.
  вывод сессии, второй: единственное место, где у посредника нет соперника, — ОТВЕТ ИНСТРУМЕНТА. Файл можно сузить до попадания в контекст, ответ инструмента приходит целиком и сузить его нечем: nodeNames не режет contextData у грязных воркфлоу (7.6), а `get_workflow_details` и подобные отдают то, что отдают. Проба В: 1 100 Б вместо 6 800 Б, выигрыш 5,7 раза, и правилом 7.6 он недостижим.
  вывод сессии, третий: посредник не дешевле, он обменивает расход подписки на место в контексте. Проба В отдала 85 042 токена расхода, чтобы освободить около 1 540 токенов контекста — отношение около 55 к 1. Финиш этого проекта измеряется контекстом, поэтому обмен допустим, но правило, которое молча множит расход на 55, владелец должен вводить зная это.
  отвергнуто: 1, сессией — требовать от правила замера расхода посредника при каждом вызове. Харнесс сообщил расход только у одной пробы из трёх: у посредников проб А и Б его не было вовсе. Правило не может требовать измерять то, что не всегда сообщается, а наращивать вокруг работы приборы наблюдения владелец запретил прямо.
  не решено: третий класс провенанса. Р15 second-brain-engineer знает два — ПРОЧИТАНО В ЭТОЙ СЕССИИ и ВСПОМНЕНО, «третьего нет», и держит правило «кто читал источник, тот и прав». Ответ посредника ни то, ни другое: источник читал не тот, кто утверждает. Три ответа из трёх сегодня оказались верны, но верность выборки из трёх — не контракт. Пока класса нет, ответ посредника в канон записывать нельзя без собственной проверки, и именно проверка уничтожает выигрыш на файлах.
  кто подтвердил: Андрей, 2026-09-24: «Начинай» — на предложение снять замер к8 прежде решения.
  обратимость: замер ничего не изменил; ни одна проба ничего не записывала.

Р-26 | В СИЛЕ | 2026-09-25 | рычаг к8 закрыт: правило посредника в силе, приёмка по объекту
  принято: Владелец внёс позицию AG-15 в `second-brain-engineer` SKILL.md двумя правками: новый пункт Р21 «Чтение через посредника» и третий класс провенанса ПРОЧИТАНО ПОСРЕДНИКОМ в Р15 вместо прежнего «Третьего нет». Приёмка выполнена этой сессией по объекту — чтением живого Skill, не по отчёту: файл 19 110 → 22 116 → 22 208 Б; Р21 на месте и побайтово равен выданному тексту; в Р15 стоит третий класс; строка «Третьего нет» в файле не встречается ни разу, «Четвёртого нет» встречается один раз. Позиция AG-15 получила статус ВНЕСЕНО 2026-09-25 с адресом и величинами приёмки. Рычаг к8 в PLAN.md переведён из кандидата в сделанное с сужением, которое дал замер: на файлах посредник не добавляет ничего сверх правила 7.6, его единственная ниша — ответ инструмента, сузить который нечем (5,7 раза), и цена ниши — обмен расхода на место в контексте около 55 к 1.
  почему: слово владельца «habe erledigt» — правки внесены; Р15 требует приёмки только независимым перечитыванием объекта, и слово владельца ею не является, каким бы надёжным ни было.
  попутно, исправлено этой сессией: правило ведения в шапке очереди, переписанное 2026-09-24 вывозом к16, утверждало, что внесённая позиция «после чего уезжает в архив». Утверждение неисполнимо для сессии, работающей только Каналом: вывоз строки идёт маршрутом 9.4(б) из сессии с открытым push, а Канал append-only файл не перезаписывает. Формулировка исправлена на «при следующем вывозе», с указанием, что вывоз копится и идёт партиями, а сессия на Канале только ставит статус. Класс ошибки — правило, описывающее возможность транспорта, написано без зонда транспорта (И-3); написала его эта же линия сессий сутками ранее.
  кто подтвердил: Андрей, 2026-09-25: «habe erledigt».
  обратимость: дёшево — все три правки этой записи суть по одному Git Replace, прежний текст в истории коммитов.

Р-27 | В СИЛЕ | 2026-09-25 | рычаг к9 закрыт как поглощённый, цель к13 снята; что осталось в плане
  принято: (1) Рычаг к9 «узкие читатели n8n» закрыт без единой постройки: обе его половины уже решены другими правилами. Половина «сигнатура входа воркфлоу вместо get_workflow_details» решена дважды — реестром `registry/tools.md`, который держит сигнатуры Канала и снимает нужду в вызове вовсе (рычаг к5), и, для воркфлоу вне реестра, пунктом Р21 second-brain-engineer: ответ инструмента и есть единственная ниша посредника (Р-25). Половина «раздел markdown по заголовку одним вызовом» решена правилом 7.6 second-brain-git: Git Read Lines с pattern, в среде с клоном grep. Строить и содержать воркфлоу ради того, что уже работает, значило бы завести второго носителя одной функции. (2) Цель рычага к13 — «путь к 15 КБ обязательной части» — перестала существовать вместе с числом 15 КБ, снятым Р-22. Сам приём, таблицы решений вместо прозы, в силе и применяется при правках Skill, но отдельным рычагом с состоянием не ведётся. (3) В плане остались два кандидата, оба требуют владельца: к10 (тонкий мастер-промпт — аккаунтный слой, правит только владелец, и правки мастер-промпта SYS уже ждут его отдельным пунктом STATE) и к12 (ответ в чат итогом, развёрнутое в файл).
  почему: замер Р-25 переопределил границу между рычагами. Пока не было известно, что посредник выигрывает только на ответе инструмента, к8 и к9 выглядели разными мерами; после замера видно, что они об одном — об объекте, который нельзя сузить до попадания в контекст, — и к8 решает это без постройки, а к9 с постройкой.
  вывод сессии, проверяемый перечнем рычагов PLAN.md: из шестнадцати рычагов сделано или решено иначе четырнадцать; открытых кандидатов два, и оба в аккаунтном слое или в поведении сессии, а не в файлах канона. Самый дорогой остаток старта — два SKILL.md, и резать их до переноса INDEX в repo запрещено следствием Р-24. Значит следующий разумный шаг проекта — не искать рычаги, а замерить старт заново и сверить его с финишем, который Р-22 определил как стоимость старта в токенах у названного читателя.
  отвергнуто: 1, сессией — вести к13 отдельным рычагом ради приёма без цели. Рычаг без числа и без читателя — то же, что число без читателя: не норма, а привычка.
  кто подтвердил: вывод этой сессии при закрытии к8 по слову владельца «habe erledigt» 2026-09-25; решение по к10 и к12 за владельцем.
  обратимость: дёшево — состояния рычагов в PLAN.md, по одному Git Replace.

Р-28 | В СИЛЕ | 2026-09-25 | приёмка закрытия AE-1; столкновение 4б.5 с 4.5 при закрытии задачи лаборатории
  принято: (1) Приёмка закрытия позиции AE-1, первый адрес, выполнена этой сессией по объекту с origin/main: файл `labs/sys/t-master-prompt-sys.md` удалён; `labs/sys/INDEX.md` 6 247 → 5 919 Б, и diff против прежней версии показывает ровно одну удалённую строку и ни одной добавленной — то есть перенабор файла целиком вышел побайтово чистым; статус AE-1 «ЗАКРЫТО 2026-09-25 (первый адрес)», второй адрес позиции — правка воркфлоу Git Read Batch отдельным нарядом — остался открытым; коммиты f8b860f и eaf6ad2. Четыре якоря мастер-промпта проверены не этой сессией, а сессией SYS, у которой промпт был в контексте как исполненный: для мастер-промпта это единственно возможная приёмка по объекту, потому что инструментом он не читается. (2) Вскрыто столкновение двух правил контракта, и сессия SYS напоролась на него не по небрежности: 4б.5 `references/lab.md` ТРЕБУЕТ закрывать задачу одним вызовом Git Commit Files, включающим и файл задачи, и правку INDEX.md, а запись двумя вызовами прямо ЗАПРЕЩАЕТ; Git Commit Files принимает файл целиком, значит модель обязана перенабрать существующий текст INDEX.md, что столь же прямо запрещает 4.5, а 8.3 велит править фрагмент через Git Replace. Выхода без нарушения одного из двух правил у сессии не было, и она выбрала атомарность.
  почему: приёмка по слову запрещена Р15, и именно на этом классе сгорела позиция U-1. Проверять чужой отчёт дешево: diff одного файла против родителя коммита стоил один вызов.
  вывод сессии, проверяемый сегодняшним diff: запрет 4.5 стоит не на размере текста, а на его происхождении, и различение тут возможно точное. Класс Р-79 — 244 версии LOG_ARCHIVE.md и потеря 69 номеров — родился там, где сессия восстанавливала текст, которого в её контексте НЕ БЫЛО. `labs/sys/INDEX.md` — обратный случай: он читается целиком в пакете 1 на каждом старте сессии SYS, то есть его точный текст пришёл в контекст ответом Канала в этой же сессии, и убрать из него одну строку — не восстановление по памяти, а вычёркивание из наличного. Поэтому сегодня и вышло побайтово чисто. Предложение по разрешению столкновения — позиция AG-16 очереди.
  отвергнуто: 1, сессией — признать действие сессии SYS нарушением и потребовать переделки. Оснований нет: правило, которое она исполнила, требовало именно этого, результат проверен побайтово, а наказывать за исполнение действующего правила значит учить сессии не исполнять правила.
  кто подтвердил: Андрей, 2026-09-25 — передал отчёт сессии SYS на приёмку.
  обратимость: ничего не откатывается; прежняя версия INDEX.md и удалённый файл задачи лежат в истории по ref родителя коммита eaf6ad2, как и предусмотрено 4б.6.

Р-29 | В СИЛЕ | 2026-09-25 | посредник проверяем через адрес доказательства; посредник по умолчанию — Sonnet; внешние исследования — отдельная папка repo; новая тема «взаимодействие Андрей — Система»
  принято: (1) Ответ посредника несёт при каждом факте адрес доказательства: для ответа инструмента — имя узла и путь JSON или цитату до 80 символов, для файла — путь и номер строки; нет адреса — NOT_FOUND с перечнем мест, где искал. Замер этой сессии, одно задание: два воркфлоу через get_workflow_details (W2lw8WIAIrwoPDUT, Nzgy6Q1XwXQVxt1F), по четыре факта о каждом, правильные ответы — из registry/tools.md. Haiku без адреса — 7 из 8, ложное «не найдено» об узле загрузки Git Append File, 67 020 токенов; Haiku с адресом, без подсказки в вопросе — 8 из 8, 67 691 токен. Тяжёлый случай — get_execution 68923 воркфлоу gdocs-read целиком, includeData без nodeNames, шесть вопросов: Sonnet и Haiku с адресом — 6 из 6 каждый, ответы совпали между собой и с тем, что эта сессия прочитала сама; Sonnet 90 677 токенов, 8 вызовов, 40 секунд; Haiku 73 862 токена, 34 вызова, 92 секунды. (2) Проверка ответа посредника зависит от источника. Источник — файл: адрес проверяется одним grep или Git Read Lines с pattern. Источник — ответ инструмента, который не сужается: сверка двух независимых посредников разных моделей, потому что сужение nodeNames у грязного воркфлоу приносит тот же объём — вызов этой сессии get_execution 68923 с nodeNames 07_Respond вернул текст документа 20 646 Б дважды, в runData и в contextData узла 05A_Export_Doc. (3) Модель посредника по умолчанию — Sonnet; Haiku — только буквальное извлечение (имена, поля, ID, значения) и только с адресом доказательства. Отказ этой же сессии от Sonnet часом раньше, по сравнению токенов 91 910 против 86 534, был ошибкой: лимит подписки считается с весом модели, справка Anthropic «Models, usage, and limits in Claude Code»: «Opus costs several times more per turn than Sonnet, and Sonnet more than Haiku». (4) Вопросы — пакетом одному посреднику: посредник с одним воркфлоу стоил 61 374 токена, с двумя — 67 020; второй вопрос обошёлся примерно в 10 процентов первого, основная цена — собственный старт посредника. (5) Отчёты внешних исследований (Perplexity) живут в отдельной папке репозитория и читаются через файл-список: одна строка на отчёт — вывод, степень доверия, адрес. Папка не заведена; заведение идёт вместе с правкой карты репозитория в Skill second-brain-git. (6) Отчёты передаются в чат вложением файла, а не ссылкой на Google Doc: документ 20 646 Б через gdocs-read пришёл в контекст дважды, вложение приходит один раз. (7) Новая тема «схема эффективного взаимодействия Андрей — Система»; итог — инструкция для Андрея; обсуждение — в отдельном чате. Кандидаты решений, собранные этой сессией: итоговый ответ в четырёх блоках — проблема, причина, методы, что делать Андрею — без сокращений и голых номеров пунктов; вопросы пакетом в начале работы, одним окном; правило Anthropic из «Best practices for Claude Code» — поправили больше двух раз по одному вопросу, значит записать вывод и начать новый чат (у Андрея это эстафета); отчёты внешних исследований вложением; ревизия denken как фильтр перед итоговым ответом.
  почему: слово владельца 2026-09-25 «твоя задача — ПОИСК РЕШЕНИЙ» и «Проверяй дальше»; отчёт Perplexity (Google Doc 1woTnFdDiytfIbJAsfrDZY3CUztAsQfyPUTj_AdDrNo8, 20 646 Б, checkSum 72a41b93) назвал главной слабостью малых моделей «тихую уверенность» и ложное «не найдено» при буквальном чтении вопроса, а первая проба Haiku показала ровно это.
  отвергнуто: 5 — (а) соотношения расхода лимита из отчёта (Opus в 10–15 раз дороже Sonnet, один ход Opus — 30–50 процентов пятичасового окна): пересказ Reddit, справка Anthropic даёт только «several times»; (б) таблица тестов из отчёта: противоречит себе — худший показатель у Sonnet при оценке «самый сбалансированный»; (в) проверочные хуки вокруг посредника: в Claude Project их нет, и это контрольный механизм, который владелец отвергает; (г) учёт токенов через Agent SDK: не среда владельца; (д) вынос обоснований из файлов старта: около 1 000 токенов выигрыша, и мешает намеренное правило 0.4.1 core/R-06a.md хранить опровергнутый диагноз рядом с исправлением.
  не решено, за владельцем: два вида ссылок на записи журнала в STATE.md и PLAN.md — «прочитать до работы» и «основание»; по буквальному правилу чтения журнала сессия этого проекта обязана открыть 22 записи, 79 163 Б, и это верхняя граница, а не наблюдение; заголовок записи журнала — вывод, а не тема: 12 из 12 первых заголовков этого журнала — темы; правило приёма новых пунктов Skill — по умолчанию в references/.
  допущение: выборка — по одному запуску на вариант; модель, реально обслуживавшая посредника, не доказана — отчёт называет случаи молчаливой подмены модели у субагентов; различие поведения и расхода между пробами говорит о том, что указание модели сработало.
  кто подтвердил: Андрей, 2026-09-25: «Считаю, что Вики должна жить в отдельной папке в репозитории», «просто запиши идею»; пункты 1–4 и 6 — вывод этой сессии, внесение в Skill — по слову владельца.
  обратимость: дёшево — кроме этой записи, ничего не менялось.