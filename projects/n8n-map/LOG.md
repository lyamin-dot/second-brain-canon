projects/n8n-map/LOG.md

Журнал проекта n8n-map: решения и тупики, только дописывается. Форма записи — second-brain-git 11. Читается оглавлением (шаблон `^(Р|Т)-[0-9]+ \|`), затем нужная запись по номеру.

Р-1 | В СИЛЕ | 2026-09-24 | носитель снимка сведений о воркфлоу (вопрос О-1)
  принято: вариант Б PLAN.md §4. Открытая часть — файл registry/n8n-map.md в публичном repo: всё, кроме путей вебхуков, признака их аутентификации и имён credential. Закрытая часть — строки HOOK и HOOKCALL в Google Doc N8N_REGISTRY 1zdz12-cwFZyHaHkKA8tJ1MvQKGCMN4xZdYztfYS3SOc, который уже перезаписывает N8N Registry Sync gFFiBPm1OomOzJOj.
  почему: 13 из 14 вопросов PLAN.md §2 решаются одним grep клоном или одним вызовом Git Read Lines с pattern. Пути вебхуков в публичный файл не идут: вебхук gdocs-read открыт (зонд Z-5 наряда Н-23.09-06), открытых вебхуков вне Канала 13 (Р-107 журнала git-migration), домен инстанса назван в registry/projects.md. Попавшее в историю публичного repo оттуда не удаляется.
  кто подтвердил: Андрей передал решение сессии словами «реши сам /denken» в чате 2026-09-24; решение принято после ревизии denken Р3 (запись Р-4).
  отвергнуто: А — всё в repo: публичная карта открытых входов, будущий откат аутентификации виден наружу со следующего утра. В — приватный репозиторий: клон без учётных данных невозможен, а Канал привязан к second-brain-canon константой REPO в узле 02_Parse_Payload воркфлоу 7OQh206m8xrZAzPF (Z-7). Г — только Doc: gdocs-read оставляет полный текст документа в contextData при любом диапазоне строк (registry/tools.md, exec 68199), поэтому каждый вопрос стоит всего документа.
  обратимость: до первой записи в repo (фаза Ф2) — полная, решение меняется правкой PLAN.md. После записи всё опубликованное остаётся в истории git навсегда.

Р-2 | В СИЛЕ | 2026-09-24 | как проверить, какие поля отдаёт публичный API n8n (вопрос О-2)
  принято: отдельного зонда нет. Первый прогон доработанного Registry Sync — пробный (фаза Ф1): ветка записи в repo выключена константой, строитель пишет в N8N_REGISTRY кандидат открытой части и строку api_fields — ключи верхнего уровня ответа и ключи узла, в том числе есть ли nodes, description, versionId, activeVersionId, credentials. Нет description ни в списке, ни в ответе по ID — стоп до решения владельца.
  почему: строитель и так обязан разбирать ответ API. Строка api_fields даёт те же сведения, что отдельный зонд, без второй правки инстанса и без ручного труда владельца.
  кто подтвердил: Андрей передал решение сессии словами «реши сам /denken» в чате 2026-09-24.
  отвергнуто: вариант а — владелец сам вызывает GET /api/v1/workflows?limit=1 своим ключом: ручной труд владельца ради сведений, которые даст пробный прогон. Вариант б — временный воркфлоу-зонд: две лишние правки инстанса (создать, архивировать) и привязка credential руками владельца (R1a n8n-engineering-manual).
  допущение: ответ API по ID (GET /api/v1/workflows/{id}) содержит nodes. Опровергается строкой api_fields пробного прогона.

Р-3 | В СИЛЕ | 2026-09-24 | бюджет чтения PLAN.md и английская копия для агента (вопрос О-3)
  принято: (1) PLAN.md этого проекта получает бюджет чтения до 48 КБ по second-brain-git 4.6; объявлено в шапке PLAN.md. Целиком файл читает один раз сессия фазы Ф1, остальные — только свой раздел по заголовку. Разделы 1 и 9 (замеры 2026-09-23) удаляются при закрытии Ф2 как устаревший снимок чисел (5.1); их текст остаётся в коммите 2d482636. (2) Английской копии PLAN.md нет.
  почему: (1) Вынос разделов 1 и 9 сейчас сберёг бы около 11 КБ из 42 (раздел 1 — 6 459 Б, раздел 9 — 4 761 Б), а PLAN.md остался бы втрое над бюджетом — основной вес в модели данных (§3) и фазах (§8), которые нужны сессии Ф1 целиком. Перенос их текста в LOG.md — набор уже существующего текста моделью (4.5); маршрут 9.4(б) закрыт: git push --dry-run в этой сессии 2026-09-24 отбит прокси. (2) Английская копия — второй носитель того же документа (И-1). Две версии разойдутся так же, как разошлись PASSPORT_MAP и description (0 из 8, раздел 1 PLAN.md редакции 2d482636) — ровно тот класс дефекта, против которого заведён проект.
  кто подтвердил: Андрей передал решение сессии словами «реши сам /denken» в чате 2026-09-24.
  отвергнуто: оставить бюджет 10 240 Б и позицию в Очереди по 5.5 п. 3 — число, которое заведомо нарушается, ничего не ограничивает. Заменить разделы 1 и 9 указателем на коммит уже сейчас — удаление ради бюджета, пока снимок не устарел, ближе к ужиманию (5.1), чем к разбору по роли.
  обратимость: дёшево — строка бюджета в шапке PLAN.md правится одним Git Replace.

Р-4 | В СИЛЕ | 2026-09-24 | итог ревизии denken Р3 кандидата n8n-map
  принято: вердикт решаемости — решение есть, не вырожденное. Исправления в PLAN.md: (1) ID документов извлекаются только из ключей, несущих ID по смыслу (documentId.value, fileId, folderId, spreadsheetId, сегмент url после /d/ или /files/, ключ fileId или documentId в jsonBody), и только значения ровно 33 или 44 символа; регулярное выражение по свободному тексту jsCode и jsonBody снято. (2) Перед первой записью в repo — пробный прогон (Р-2) и защита в коде: строка открытой части с /webhook/, http, Bearer, @ или путём вебхука роняет прогон. (3) get_execution с includeData:true по Registry Sync запрещён. (4) Поле project отложено. (5) Три строки-указателя в Skill выдаются владельцу в той же сессии, что закрывает Ф2. (6) Отдельная фаза Ф1-зонд снята; Ф1 теперь — доработка строителя с пробным прогоном, Ф2 — первая запись в repo.
  почему: (1) Обратный ход (triz --brief) нашёл провал, который план уже делал: регулярка на 44 символа по тексту Code-узлов принимает 44-символьную подстроку ключа доступа за ID документа и публикует её навсегда. (2) Первая запись в публичный repo необратима, а прежний план проверял результат только после записи. (3) Узел httpRequest оставляет сырой ответ API в contextData при любых nodeNames (R4 n8n-engineering-manual), а сырой ответ — полный JSON всех воркфлоу инстанса. (4) Вывод проекта требовал нового узла Drive API, привязки credential руками и доступа сервисного аккаунта ко всем паспортам; на вопрос В8 отвечает сам паспорт, ID которого даёт строка W. (5) Ф2 меняет содержимое N8N_REGISTRY, а три Skill называют его носителем живого состава: без правок в тот же день сессии наутро ищут список не там.
  кто подтвердил: Андрей передал решение сессии словами «реши сам /denken» в чате 2026-09-24.
  отвергнуто: полный аппарат ТРИЗ — хватило противоречия и разделения; противоречие «снимок должен быть публичным ради дешёвого чтения и не должен быть публичным ради безопасности» развязано разделением в пространстве (две части по чувствительности полей) и во времени (пробный прогон до публикации).
  допущение: белый список ключей пропустит часть ID (записанных под другим ключом или в jsCode) — на вопрос В1 снимок ответит неполно, без признака неполноты. Принято как меньшее зло против утечки ключа в публичный файл.

Р-5 | В СИЛЕ | 2026-09-25 | как провести фазу Ф1 после закрытия Н-23.09-03
  принято: (1) Ф1 меняет один узел «Построить реестр» воркфлоу gFFiBPm1OomOzJOj черновиком: update_workflow без публикации, затем ручной прогон черновика через MCP. Новых HTTP-узлов в Ф1 нет; узлы git-read и git-write и привязка к ним Header Auth руками владельца переходят в Ф2. (2) Старый текст реестра идёт в N8N_REGISTRY первым без изменений, кандидат снимка — ниже него. (3) Защита открытой части ищет `http:` и `https:`, а не подстроку `http`. (4) Белый список ключей ID дополнен ключом `documentURL` и режимом list у `documentId`. (5) Ф1 исполняет сессия сама (naryad 3.9, маршрут Б, вопрос 4: результат Ф1 — вход Ф2) после «да» владельца по second-brain-engineer Р10.
  почему: (1) Протокол записи n8n-engineering-manual 2.1: update_workflow не публикует, черновик живёт до publish_workflow; execute_workflow в режиме manual исполняет черновик. Запуск 05:30 продолжает исполнять опубликованную версию d73a09ea, поэтому пробный прогон её не трогает. Узлы записи в repo при выключенной записи не нужны, а их привязка руками — единственный ручной шаг владельца в прежней Ф1. (2) Три Skill читают N8N_REGISTRY как список живых воркфлоу; до прогона 05:30 они получают прежний список. (3) Локальный прогон кода на тестовом наборе 2026-09-25: подстрока `http` роняла каждую строку D с типом узла httpRequest и каждую строку C с типом httpHeaderAuth. (4) Тот же прогон: узел Google Docs хранит ID документа в ключе `documentURL`; режим list у `documentId` хранит тот же ID, что режим id.
  кто подтвердил: решение сессии в рамках плана, утверждённого словами Андрея «реши сам /denken» 2026-09-24 (Р-1…Р-4); правка воркфлоу ждёт отдельного «да».
  отвергнуто: наряд на Ф1 другой сессии — провёз бы готовый код узла дважды и отдал результат Ф2 через шину; публикация в Ф1 по прежнему паспорту (unpublish, publish) — меняет исполняемую версию ради прогона, который черновик даёт без этого.
  допущение: update_workflow сохраняет привязку credential у двух существующих HTTP-узлов. Опровергается пробным прогоном: ошибка узла «Получить список воркфлоу n8n» или документ N8N_REGISTRY не перезаписан.
  обратимость: полная до Ф2 — черновик заменяется следующим update_workflow, опубликованная версия не меняется.

Т-1 | В СИЛЕ | 2026-09-26 | пробный прогон Ф1: паспорт из ответа API не выводится
  что пробовали: черновик 4792bd9d-3752-45aa-9226-c1cbe6e32ba7 воркфлоу gFFiBPm1OomOzJOj (узел «Построить реестр» по Р-5, опубликованная версия d73a09ea не менялась), ручной прогон 69341 — success за 6 секунд, N8N_REGISTRY перезаписан; документ прочитан через Google Drive read_file_content и разобран скриптом в среде сессии.
  результат: строка api_fields — ответ списка содержит nodes, versionId, activeVersionId, activeVersion, settings, shared, tags, у 662 из 1981 узлов credentials с типом и именем; description — 0 из 197. Кандидат открытой части — 726 строк, 61 973 байта; адресов, «Bearer», «@», пути /webhook/ и строк длиннее 44 символов в нём нет. Защита сработала трижды, все три раза на имени воркфлоу, содержащем путь вебхука: B5jkqW0bUScxmTi7 (gdocs-replace-v2), J8tubuqNsN471jEl (gdocs-read), fdiWemSCln1WA8gt (gdocs-apply-from-file). Все 26 вебхуков инстанса несут headerAuth; строка HOOK для gdocs-read совпадает с triggerInfo. Строки DoD фазы Ф2 для D (N8N_REGISTRY) и X (5MDOGVCTxwDjHqy3 → aVpgEnYBAgIIss4k) в кандидате есть. Допущение Р-5 устояло: update_workflow привязку credential у двух HTTP-узлов не сбросил, вопреки записи в паспорте воркфлоу о трёх сбросах при разработке.
  почему тупик: поле passport строки W (вопрос В4 PLAN.md §2) из ответа списка не выводится — стоп-условие Р-2. Ответ по ID (GET /api/v1/workflows/{id}) не проверялся.
  куда дальше: Р-6.

Р-6 | В СИЛЕ | 2026-09-26 | паспорт в снимке при отсутствии description в ответе API (вопрос О-4)
  принято: (1) поле passport из строки W снято; носитель паспорта — строка PASSPORT в description воркфлоу, читается вызовом search_workflows, который отдаёт description всех воркфлоу одним ответом (около 84 КБ). Ф3 сверяет паспорта с description по search_workflows. (2) Защита открытой части в Ф2 не роняет запись на имени воркфлоу: путь вебхука внутри имени маскируется в открытой части, закрытая часть держит имя целиком; прочие проверки защиты действуют на имя как прежде.
  почему: (1) Т-1: description в ответе списка нет ни у одного воркфлоу. Отдельная проверка ответа по ID — ещё одна правка черновика ради поля, которого, вероятно, нет и там. Копия паспорта в снимке была бы вторым носителем того же факта (И-1). (2) Т-1: все три срабатывания — имена, выбранные владельцем; все 26 вебхуков закрыты заголовком, но путь gdocs-replace-v2 в публичном repo до сих пор не назван, поэтому маскировка, а не снятие проверки.
  кто подтвердил: Андрей словами «Согласен убрать паспорт из снимка» в чате 2026-09-26 — пункт (1); пункт (2) — решение сессии в рамках плана.
  отвергнуто: проверка GET /api/v1/workflows/{id} новой правкой черновика — см. «почему» (1); снятие проверки пути с поля имени — публиковало бы путь gdocs-replace-v2.
  обратимость: дёшево до Ф2 — правкой PLAN.md и кода узла; после Ф2 поле возвращается следующим прогоном, если API начнёт отдавать description.
