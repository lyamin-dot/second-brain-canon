labs/sys/t-prompt-backup.md

# Мастер-промпты нигде не резервируются

Суть: тексты мастер-промптов Claude-проектов существуют в одном экземпляре — в интерфейсе Anthropic. Реестр хранит URL проекта, Skill описывает эталон поведения, но фактический текст промпта не лежит нигде: ни на Drive, ни в repo, ни в бэкапе. Промпт при этом решает, что сессия читает на старте и какому контракту подчиняется.

Следующий шаг: решение владельца — резервировать или явно отказаться. Если резервировать, носитель определяется по правилу выбора: текст, который правит только человек и читает только человек, не обязан ехать в repo.

Условие снятия: копия каждого действующего мастер-промпта лежит по названному адресу, либо отказ записан с обоснованием.

Ссылки: `registry/projects.md` (URL проектов); источник — Зона А SYS_INDEX, строка 265; дата появления в источнике не проставлена. Тот же класс, что единственный экземпляр текста Skills — позиция J-4 очереди обслуживания.
